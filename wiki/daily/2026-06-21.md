---
tags:
  - domain/3d
---

# 2026-06-21

Camera work continues on `feat/camera-mobile-framing`. Today: comparison tables (app vs lab), then
porting proven lab mechanics into the real app + a new in-app controls cheat-sheet. Full port record:
[[camera-lab-to-app-port]].

## Shipped / committed

- **Desktop + mobile control tables** (app vs lab) — confirmed desktop is converged; the two real
  deltas were the near-horizon pan cap and mobile Focal-Y. Drove the day's ports.
- **Side-view diagram in the app** — `CameraDiagram` relocated `camera-lab/` → `components/scene/`
  (production home, `className` prop for placement); new `CameraSideView` DOM overlay reads a per-frame
  `lib/scene/cameraReadout` written by `DreiSceneControls`; toggle in Orbit settings (`showSideView`).
  Stacks above the seed bar (bottom-left).
- **1-finger-on-pin Focal Y (touch)** ported into the app touch handler; `pinScreenHit` gained a touch
  pad. **Sticky Focal-Y = 0 detent** (`snapFocalY`) ported into BOTH mouse + touch scrubs.
  **Default Focal Y set to 0** (`DEFAULT_ORBIT.lookAtY` 120 → 0) — default now aims at the ground.
- **Controls cheat-sheet** (`components/ui/ControlsGuide`) — the "?" button, bottom-right, NON-MODAL
  (no scrim; stays up while you test; Esc / ✕ / "?" dismiss). shadcn Tabs split Mouse / Touch and
  auto-switch to the last input used. The active gesture's row **highlights amber live**
  (`lib/scene/cameraActivity` marked per-gesture in `DreiSceneControls`). Glyphs are editable SVG
  assets in `public/controls/*.svg` (mouse buttons / wheel, 1-3 finger, pinch, motion arrows — the
  4-way redrawn with a centre gap so it reads as arrows, not a crosshair). Mounted in the app AND the
  lab. All verified headlessly (panel renders, stays up, Zoom row lights while wheeling).

## Open / next

- **Near-horizon pan limit** — the one lab mechanic still NOT in the app (the grazing pan "jump").
  Port `CustomOrbitControls.doPan`'s cap + horizon taper into `DreiSceneControls`' pan sites. See
  [[camera-lab-to-app-port]] "Pending".
- Watch the default-Focal-Y-0 framing change in practice; revisit if the ground-aim default feels low.
- Optional: live highlight + per-method controls in the LAB (the cheat-sheet shows the app scheme).

## Later — framing work + a rotate/tilt dead-end

- **Committed in 3 chunks** (local, `feat/camera-mobile-framing`): controls-guide icons + idle-fade
  chrome (`498e89c`); honest side-view diagram — real-eye glyph + elevation-true cone (`9c80d7d`);
  unify low-angle curve + fix Focal-Y scrub (continuous deadband + projection-correct scale) + ortho
  ground-clamp (eye ≥ frustum-half-height, controlled void gap) (`c330fe8`).
- **Rotate/tilt foray — REVERTED.** Tried grab-to-rotate, then a turntable + tilt-gate, to fix the
  inverted-rotate + oval-tilts gripes; both regressed the feel ("trash", then up/down orbited).
  Reverted to `c330fe8`. Write-up + lessons: created [[camera-rotate-tilt-foray]].

## Wrap — controls surface + focal-height remap

- **Controls guide → control surface** (`ControlsGuide`): ~135% larger; "Pan"→"Move"; hotkeys became
  live **switches** (Auto-Orbit, Ortho/Perspective, Show Pin, Zoom, Settings) wired to store state
  (amber keycaps; hidden on touch, labels+switches stay). Settings panel `hidden` lifted to the store
  (`panelHidden`) so the switch + `H` share it. Real lucide `MapPin` (sky-blue) replaces the hand-drawn
  pin and now renders **behind** the panels (`zIndexRange` 100→15). Touch hand glyphs redrawn — index
  left; index+middle (middle larger) for Move; thumb+index for pinch. (First redraw was a filled-fist
  regression; reverted to the original outlined-palm shape.)
- **Free-look retired on mouse → Focal Height on `LMB+RMB` / `Ctrl/⌘+LMB`** (height-only re-aim from a
  fixed eye), mirroring Move's `RMB / Shift+LMB`. 3-finger touch free-look left in code, off the sheet.
- **Focal-Y scrub fights fixed** (details in [[camera-systems-history]] Era I): relax the eye-ground
  clamp during a scrub (the near-0 fight); freeze the low-angle ground pull during a scrub + ease back
  on release (the low-angle jerkiness). Rotate untouched. **Both need a live feel-check.**

## Open / next — wrap the feature
- Feel-check the focal-Y scrub fixes (near-0 + low-angle) and the `Ctrl/⌘+LMB` focal-height feel.
- **Merge `feat/camera-mobile-framing` → `dev`** once the above feels right (the agreed wrap-up).
- Rename `DreiSceneControls` → `OrbitCameraRig` (its own commit). Decide: keep/remove 3-finger touch
  free-look; "Focal" vs "Focus" naming (kept Focal — matches Focal X/Y/Z).
