# 2026-05-27

## Window + road polish (arch/city-planning → feature branches)

Working the city-gen GitHub backlog (label `city-gen`, #9–22) on feature branches off `arch/city-planning`.

- Shipped **#15 in-scene road surfaces** (`feature/road-surfaces`): dark-asphalt ribbons from the highway + arterial polylines, polygonOffset to kill coplanar z-fight with the ground.
- On `feature/window-aa`: fixed **#9 window seams** + improved **#10 moiré** (the `fract()`+`fwidth()` derivative-spike bug — compute fwidth on the continuous grid coord), grazing "whole-panel-lit" fix (cap AA band + `min` relSpan), **#13 binary windows** + occupancy "lit ratio" / "activity" controls, **#19 settings panels closed by default**, and a live **Anti-Aliasing** panel (AA/LOD/occupancy uniforms).
- Ran a short grill-with-docs session on per-building window proportions → created [[decision-window-proportion-by-archetype]] and amended the fenestration out-of-scope line in `CONTEXT.md`. Implementing on `feature/window-aa`.
