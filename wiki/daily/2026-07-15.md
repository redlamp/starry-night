# 2026-07-15

- Merged `feat/persona-units` → `fable` (--no-ff), pushed, deleted the feature branch.
- **Camera v3 pass** on `feat/camera-v3` (off fable): built `snv3` "Starry Night Cam v3"
  as a fork of SNCv2 with the modes folded in as in-camera behaviours — T top-down
  flight (banked return + deviation re-square), idle auto-drift (toggle + delay slider,
  Settings → Orbit → Idle Drift), cone-view arc-perpendicular framing + orbit pivot
  pin/cone glyph, hand-rolled touch (1-finger orbit / 2-finger pan+pinch / double-tap).
  New default model. created [[decision-camera-v3-continuous-modes]]
- Headless verification via `cdpShot` + store probes: T round-trip pixel-exact, deviation
  re-square, drift engage/cancel, viewAzimuthDeg settle, cone pin lifecycle, touch
  orbit/pinch/double-tap. Gotcha: `?capture=1` parks `cameraMode:"still"` — set orbit in
  SHOT_SETUP first; `set/releasePointerCapture` throw on synthetic pointers → guarded.
  Gates: typecheck ✓ lint ✓ gate1 ✓.
- created [[test-plan-2026-07-15-camera-v3]] — includes the jarring/anti-convention
  watchlist (north-up loss, modal T, self-moving camera, 1-finger rotate vs pan, etc.)
  for the user's live round.
- Round 2 (user's Section 1–2 notes): T flights decomposed to moveTo/rotateTo/dollyTo
  (no long-way revolve after accumulated turns), idle delay default 30 s, auto-drift
  switch into the Idle Drift header (+ "?" beside the title), leave-inspect re-arms the
  idle timer, drift azimuth/wander clocks integrated so Speed/Revolve slider drags don't
  jump the camera, Default Orbit + side-view moved to the section bottom, labels
  tightened (Orbit/Zoom/Move/Delay/Speed), and the Map/Drift-only framing controls
  (Screen Y, ground pull, tilt/low-angle speeds) hidden while v3 is active — the user's
  2.** hunch was right. All headless-verified; fixes logged in the test plan.
- Round 3 (Section 3 + 5 notes): persona cone frames centre on the commute midpoint with
  both endpoints always in frame (3.17); pan eye-ring scales with zoom so it can't pin
  before the focal (3.21 — also the RMB pseudo-zoom source, 5.30); wheel ×1.6 + eased
  steps; focus glide slowed to ~1.5 s (5.31); projection-toggle radius hold extended to
  v3's in-model top-down + drift via cameraCommand.projectionRadiusHold (5.32, the #84
  guard only knew the Top-Down MODEL); "Default Orbit" removed, diagram toggle relabelled
  "Diagram"; idle-drift wander 0.8 default and re-anchored to wander the CITY CENTRE
  (eased in from the parked focal). Headless-verified; logged in the test plan round 3.
- Round 4: drift takeoff reshaped from pose-blend to VELOCITY ramp (all rates 0 → full
  over 10 s; focal follows the centre-wander path's velocity + a ≤30 m/s capped pull),
  removing the boot-idle lerp; and a 520 m cruise-altitude floor (above the ~480 m
  worst-case skyline) lifts the drifting eye clear of towers on any seed. Profiled via
  CDP: standstill first 2 s, no jumps, eye 160→~630 m. Logged in test plan round 4.
- Round 5: double-tap/-click zoom is a glide in both projections (ortho size on a 0.7 s
  eased ramp instead of the instant set; perspective bracketed in a slower smoothTime)
  and the browser-synthesized dblclick after a touch double-tap is de-duped (the double
  fire was the "snap", 4.27). Drift wander clarified per user: the city centre is a
  BOUND (roam disc, widened to the start distance when engaging outside it), never an
  attractor — round 4's convergence pull removed. Test-plan rounds + Section 6 now carry
  checkboxes for the user's pass.
- Settings → Labs: new bottom section listing the workbench pages (Writing/Camera/Drei/
  Window Lab, Palette, Plan View, Tensor Field) as new-tab links with external-link
  icons; the Writing Lab button left the ControlDock (its 2026-07-12 home). Later:
  Tenancy Layout added (moved docs/ → public/prototypes/ so the app serves it; 2-col).
- Filed #95 (compass rose in v3 top-down). **Shipped** feat/camera-v3 → fable → dev →
  main (--ff-only), tagged v2026.07.15; Pages deploy green. Deleted the feature branch.
- Round 6 (post-ship, on fable): Orbit header play/pause now drives v3's **auto-drift**
  (one transport whatever the model; other models keep orbitPaused), and auto-drift is
  **OFF by default** (watchlist 6.4/6.5). Verified: boot-idle stays still; enabling
  starts the ramped drift.
