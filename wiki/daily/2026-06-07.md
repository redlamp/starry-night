# 2026-06-07

## Small hours — facade controls arc (dev)

Continuation of the 06-06 late-evening facade work, wrapped and shipped as
`v2026.06.07`.

- **Whisper defaults**: user tuned facade ranges live — sat 0.02–0.08,
  light 0.02–0.06. Silhouettes lift off the sky; walls don't read as
  coloured. The louder look is recoverable from the curated stills.
- **Buildings panel split**: Windows + Facade as collapsible sub-groups
  (`SubGroup` — SubHeader + chevron, transient open state; Windows defaults
  collapsed, Facade open).
- **Facade controls**: warm % (family coin), warm/cool hue ranges on
  hue-spectrum-painted tracks (degrees), sat + light min/max ranges. All
  live through the #55 source arrays — no rebuild. Control order: hue
  family first, then sat/light.
- **Lights kill switch** on the Windows sub-header (`uLightsOn`): every
  window dark, facades only — the facade-debug view. Not persisted; Reset
  turns it back on.
- **Verification infra**: `window.__sceneStore` exposed in capture mode;
  one-off Playwright checks drive `setFacade`/`setWindowLights` headlessly
  and screenshot scene + panel (kept in gitignored `samples/*.ts`).
- created [[decision-facade-display-space-color]]; linked it +
  [[decision-correlated-window-lighting]] (missed yesterday) from the
  decisions MOC; display-space rule added to `CLAUDE.md`.

## Daytime — #55 tile-cull visualization + issue grooming (dev)

- **Debug View → Tile culling** panel block: tile-grid overlay (depth-tested
  translucent AABBs over a canonical all-buildings partition; green =
  materialised, red = evicted), **freeze cull frustum** switch
  (`freezeCullCamera` pins the shared cull matrix — all three consumers keep
  culling against the captured pose while the camera flies out to inspect
  eviction), live per-layer readout (tiles + items, 500 ms poll on
  `lib/scene/tileCullDebug.ts` counters). Transient debug state, never
  persisted.
- `__tileCullDebug` exposed in capture mode next to `__sceneStore`;
  `scripts/verifyTileCullOverlay.ts` e2e — culling bites (111/182 tiles,
  4050/5587 items at default orbit pose), freeze pins during fly-out,
  unfreeze re-follows. PASS; gate1 + tileCullSanity PASS (gen untouched).
- Issues: #55 notes updated (tooling comment); #26 retitled to
  "implementation pass (research done)" + queued behind #49; **#49 confirmed
  next big task** — spike `feat/suburbs-density` stays parked, rebuild per
  [[plan-metro-suburbs-highways]] Stage-0 review feedback, now unblocked at
  Metro extent by the 8-tier size system.
- CI non-issue: /dev → dev reroute was already reverted (3fa465d,
  v2026.06.03); `localhost:7827/dev` was the red herring.

## Evening — #49 suburbs + density gradient, increments 1–4 (feature/suburbs-v2)

Kicked off the #49 rebuild on `feature/suburbs-v2` (off dev, post-#55 ship).
Created [[decision-density-gradient-model]] — the load-bearing choices:

- **Density field** (`lib/seed/density.ts`): absolute-distance Clark's-law
  falloff (NOT extent-normalised — bigger tiers ADD rural/fringe bands),
  4 bands (core/suburban/rural/fringe), seeded angular harmonics for organic
  band edges. Two layers: radial (pre-district, for road gating) +
  per-district (jitter + character floors, for buildings/lamps).
- **/plan density layer**: band-filled cells + radial contour rings (toggle,
  default off).
- **Buildings**: ~150 m hash-seeded development-cell dropout (whole-parcel
  gaps, kept fabric stays FILLED — the spike's per-lot skip inverted),
  archetype tilt + size shrink toward the edge, interior fill fades to yards.
- **Streetlights**: spacing-not-brightness (×1→2.5 stretch), one-sided
  staggered zig-zag past suburbAmount 0.35, never zero; main roads exempt.
- **Roads**: arterial seed gate + separation taper (through-lines terminate at
  band edges; survivors = radial spokes), street-sep ramp (tight in suburbs so
  subdivision reads, country lanes in rural). Found via lamp telemetry that
  the arterial grid — not the lamps — was what kept the rim uniformly lit.
- Verified each increment: gate1 PASS throughout; per-band counts
  (e.g. City tier: buildings ~870/610/190/15 per km² across bands; lamps
  ~1100/600/230/60) + /plan and fog-free scene captures in
  `samples/verify49/`. Tier-3 default orbit look unregressed.

**Increment 5 — winding + cul-de-sacs + block jitter** (same evening, after
the checkpoint): the "different mechanism" the Stage-0 review demanded —
post-trace **domain warp** on minor streets (seeded plane-wave displacement,
~9–14 m at full suburbAmount, endpoint-tapered so junctions stay anchored;
parallels sample the same warp so spacing holds; arterials untouched =
straight spokes), **cul-de-sac stubs** grown off suburban minors into block
interiors (separation-checked against final warped geometry, gentle seeded
crescent curvature, ~450/seed at City tier), and **block-size jitter**
(low-freq noise × the street-sep ramp, suburbs only). Sketch cities (#40)
bypass all of it. gate1 PASS; /plan zoom shows serpentine teal streets with
stub clusters; rim scene capture reads organic (dotted wandering lamp lines,
dark gaps, straight spokes). Streamed boot-blueprint lines are unwarped
(≤14 m transient mismatch under the road reveal — invisible, noted).

Remaining on #49: warm-sparse residential windows, per-seed suburb variety
(warp amp + stub chance are seeded but narrow), rural cluster bias toward
spokes. NB: suburban archetype taxonomy referenced in
[[plan-metro-suburbs-highways]] as "captured separately" — couldn't locate it
in wiki or issues.

Parallel-session note: `4a06281` (fps-badge → Performance header) landed on
this feature branch via the shared tree.

## Late evening — #49 course-correct + look/UI feedback batch (feature/suburbs-v2)

- **User review of the branch-grower suburbs**: still too grid-bound, organic
  shapes read as "AI slop"; veered from tangential/diagonal lines connecting
  population nodes. Ran a design-agent deep-think → created
  [[plan-suburb-node-fields]]: all three attempts kept the global lattice
  topology; rebuild on **population-node radial fields** (pods of concentric
  crescents around seeded neighbourhood nodes, node-graph connectors, grid
  suppressed past the core, density clustered by node proximity). Next work
  block, 5 staged increments.
- **District gradient** (user): speckled mixed-use→residential transition
  (probability fades with radius — pockets both ways) + seeded outer
  "edge-city" subcentres (Bellevue pattern, ≤ ~2.3 km, count scales with n).
- **Ground + haze** (user): both now tier half-extent + 200 m apron, centred
  on CITY_CENTER (the old fixed 3000 m stopped tracking the tier knob).
  Briefly cut the haze sphere to a top hemisphere misreading "city on a
  globe" as a complaint — **the under-hemisphere is a liked feature** (city
  resting on half a globe; the band shader clamps to full alpha below
  bottomY, which is what draws it). Full sphere restored; only the radius
  changed. `haze.radius` setting reinterpreted as a scale on the tracked
  base (default = 1×).
- **Settings IA batch** (user): Camera header default/free pose toggle
  (snapshot + tween, transient); live readout into Camera; Roads highlight
  tri-switch on the header; Atmosphere master switch (fog+haze) on the
  header; Anti-Aliasing folded into Performance; Seed row into City Details;
  Buildings→Facade collapsed by default; **removed** the manual camera-intent
  inputs (position/orient-by/lookAt/rotation) — `locked = flying || orbiting`
  is true in every camera mode, so they were permanently disabled relics
  (store API untouched). Search keywords re-homed.

- **Node-field Stage 1 landed** (`lib/seed/suburbField.ts`): deterministic
  neighbourhood-node sampling (fixed-order grid scan, stream
  `::suburb::nodes`, 3 draws/cell + 3 per accepted node; Poisson-disc spacing
  450–1250 m by density; elliptical squash) + `nodeProximity` for Stage 4.
  /plan density layer now draws node dots + pod ellipses. gate1 PASS with
  IDENTICAL street counts (byte-stable — nothing consumes nodes yet).
  Stage-2 note: filter nodes by the shape mask (corner nodes sit past the
  circle footprint).

## Night — population layer + traffic busyness + settings polish (feature/suburbs-v2)

- **Top-down `t` fix**: `topDownFraming()` framed the fixed `CITY_HALF_EXTENT`
  (3 km look constant) instead of the runtime tier extent — a Metropolis only
  showed its central 3 km. Now `maxHalfExtent()`; verified at tier 6.
- **Population field** (user): created [[decision-population-density-field]] —
  per-building floor-area × occupancy census, 80 m grid + blur, p99-normalised,
  cached on generateCity's key. Heat-map overlay (inferno ramp, GIS-layer
  plane) + `est. population` readout (~277 k at 4 km).
- **Traffic busyness follows the area** (user): per-segment multiplier off the
  population field, saturating at pop 0.45 so downtown holds while remote
  arterials thin (224→202 / 180→99 / 77→31 cars/km² core/suburban/rural);
  highways exempt. `traffic.popCoupling` slider (default 1). gate1 PASS.
- **Settings IA round 2** (user): Districts panel → **Population** section
  (Density sub-group above Districts); shells switch on the Districts
  sub-header; hovering a district row highlights it in the scene (transient
  `highlightDistrictId`, works with shells off); section order now Intro /
  Camera / Orbit / Roads / Buildings / Population / City Details* / Stars /
  Moon / Atmosphere / Debug / Performance (*unlisted in the user's order —
  kept with the city group, flagged); camera header toggle now shows the
  CURRENT mode (default/free), not the action.
- NB: Playwright `chromium.launch()` hang recurred (memory:
  playwright-windows-launch-hang) — scene/panel shots blocked; verified via
  in-process PNG render (`samples/verify49/pop-heat.png`) + numeric band
  tables instead. Panel + scene need a live eyeball.

## Late night — #49 node-field Stage 2: pod tracing (feature/suburbs-v2)

Per [[plan-suburb-node-fields]] — the payoff increment. The suburban band's
fabric is now traced PER POD against the summed node tensor field:

- **Node tensor field** (`suburbField.ts`): each node a squashed/rotated
  radial basis, unit-normalised tensor × Gaussian, spatially indexed (3×3 of
  1280 m cells — the #63 lesson). Overlapping pods deform each other's rings.
- **Pod tracer** (`tensorStreets.ts`): rings = major eigenvector (closure
  detection → full ellipses where unobstructed), spokes = minor traced
  outward-only. Fixed 9 draws/node (`::suburb::podstreets`); seed-retry at
  fixed angle offsets so one arterial slicing a pod doesn't kill the ring.
- **Global grid band stop** (draw-free: seed reject in accept + trace
  termination, so core streams stay aligned): streets stop at suburbAmount
  0.28 ≈ density 0.48. First cut at 0.6 was a threshold→radius error — that
  IS the rim at tier 4, the grid never yielded.
- **Retired**: warpStreets, growBranches (crescents/culs), the suburban
  collector-ramp tail. Arterial separation opened further
  (suburb ring ~2.6× → pods nest between section lines).
- **Nodes**: mask-filtered (corner-pod fix), band narrowed to sub ≥ 0.18.
- Verified: gate1 PASS ×2; tier-4/6 counts (pods 20/36, rings 46/81,
  spokes 24/50); per-band gradient holds (core ~700/km² → suburban ~340–380
  → rural ~70–160, suburb avg h ~8–9 m); in-process plan render
  (`samples/verify49/stage2-tier4.png` — browserless, Playwright still
  hanging) shows grid core → pod crescents between sparse arterials.
- Buildings at tier 6 dropped ~12.9k → ~10k — the band's global-grid
  frontage went with its streets. Stage 4 (development clusters by
  nodeProximity) redistributes; suburban band may read sparse until then.
- Next: Stage 3 connectors (node→arterial/node/core desire lines), then
  Stage 4 density coupling, Stage 5 rural ring + per-seed variety.

**Stage 2 REDO — subdivisions, not rings** (user: "lots of circles… no
kidney bean / bezier curve roads; some developments don't even connect").
Root cause: a radial tensor basis traces bullseyes by construction; the
closure detection made perfect disconnected ellipses. Grounded the redo in
real planning — created [[suburban-street-patterns]] (Southworth &
Ben-Joseph taxonomy; VDOT/municipal dimensions):

- **Loops-and-lollipops builder** replaces the pod tracer: collector SPINE
  (Catmull-Rom, anchored to the nearest existing road, 2nd mouth when
  reachable) → LOOP streets (leave/return to the spine, 60–110 m bulge —
  the kidney beans) → CUL-DE-SACS (≤ ~140 m bent stubs). Connectivity by
  construction — anchors only, a pod that can't reach a road isn't built.
  Accepted geometry joins the anchor index, so pods chain.
- Retired the node tensor sampler (dead code) — nodes are now placement +
  (Stage 4) density only.
- **Bug worth remembering**: SUB_EXEMPT (34 m) < sep distances (38/46 m) →
  every curve died at its first tested point (zero subdivisions). A curve
  departs its host ~perpendicular: the anchored-end sep exemption MUST
  exceed the sep radius. Plus mirrored retries (loops/culs flip to the open
  side; spine retries ±55°).
- **Density profile widened** (user: "downtown should be surrounded by much
  more midsized non-spire blocks… suburbs at the periphery"): Clark
  exponential → generalized exponential `exp(−(r/1929)^1.5)` — flat
  shoulder, band edges core ~1.2 km / suburban ~2.24 km / rural ~3.34 km.
  Tier-6 core 6.1→8.2 km², core avg h 58.8→37.6 m (the mid-rise ring);
  tier-4 rural shrank to corner clips — suburbs own the periphery.
- gate1 PASS ×2; renders `stage2-tier4/6.png` (browserless). Subdivisions
  still sparse across the band until Stage 4 redistributes development.

**Round 3 — packed blocks + saved-config boot hydration** (user: blocks
should be "relatively packed with a network of roads that connect to each
other"; Save should persist across refresh):

- **Host-queue network grower**: the spine seeds a queue; accepted BRANCH
  collectors and LOOPS join it, so streets sprout off every generation until
  separation says the land is full (curve budget 12–42 by density). Branch
  tips that land near another road SNAP onto it — real interconnections.
  Node spacing tightened (suburban 520/430 m); branch len ≤ 300 m.
- **R0 1929 → 1768**: the first widened profile pushed the tier-4 suburb
  band mostly past the map edge (pods had no land — render showed a bare
  rim). Band edges now core ~1.1 km / suburban ~2.05 km / rural ~3.06 km.
- **Saved-config boot hydration** (`sceneStore.ts` module init): Save wrote
  localStorage but only the Revert button ever read it — every refresh
  booted defaults (the user kept re-setting city size). `revertToSaved()`
  now runs at module init, before the tier mirror sync + first gen (no
  double generation). Headless captures unaffected (fresh localStorage).
- gate1 PASS; tier-6 render = near-continuous subdivision webs through the
  suburban ring; tier-4 = packed rim plats. NB: a saved config with
  hydration COULD surface SSR hydration warnings if any panel HTML renders
  pre-mount with store-bound values — watch dev console.

## Round 4 — block-fill metric, packed pods, odd-angle core patches

- **Block-fill metric** (user: "is there a way to measure how full a block
  is?") — `samples/verify49/blockFill.ts`: % of each band's mask-clipped land
  within 70 m of a street. Baseline suburban 51–77% → after packing 74–81%
  (stations ~42–70 m, branch gen ≤ 3, area-scaled curve budget, SUB_SEP_OWN
  34, node spacing 470/410).
- **Odd-angle grid patches** (user refs: SD Barrio Logan 45°, SF Market St —
  added to [[map-layout-references]]): 1–3 district-sized (280–460 m abs)
  rotated ±30–60° patches in the core annulus. First cut as amplitude-boosted
  bases FAILED — tensor summing averages angles → warp, not seam. Now
  **override regions** in sample(): hard interior, smoothstep seam band
  0.7→1.15×size, both tensors normalised at the blend. Renders show crisp
  rotated districts with arterials bending around the seam. ≤3 early-out
  distance checks per sample (#63-conscious).
- **UI**: orbit pause → Orbit section header as "orbit/still" state toggle
  (panel-header contextual Pause/Resume removed — NB top-down's scene-clock
  pause lost its button; Space still works for orbit); Intro header action
  "▶ play" → "replay".
- gate1 PASS; minors back to pre-Stage-2 volume (2141/5 seeds) as
  subdivision fabric.

## Round 5 — fill metric → saturation, patches v2, population profile tools

- **Packing v2** (user: "still very sparse… measure how full a block is"):
  per-pod GAP FILLER — raster the pod disc +30%, grow a connector toward any
  spot >70 m from a street ("served" = global index, no double-fill);
  stations 36–60 m, branch gen ≤3, area-scaled budget, sep 32, node roll
  0.97. Suburban fill 51–77% baseline → **84/71/92/85**; tier-6 minors ×3.
- **Patches v2** (user: "radiating out from a district or bounded by an
  arterial"): regions ELONGATED along their own grid angle (the long seam
  parallels the patch streets — reads as the bounding street), ~22% RADIAL
  (star-plaza rings+spokes, plan-0 rolled one), count up (1–3, p .6/.3),
  sizes to 550 m.
- **Population profile** (user: "tools to influence the population gradient…
  mark centers, radiate, gradient curves, overflows"): `DensityProfile`
  {centres 1–3, spread, shoulder, satellite} — module mirror like
  fieldDeviation; satellites are SEEDED centres in the mid-ring; centres
  combine soft-OR so gradients OVERFLOW into conurbations. Wired: density.ts
  field, all gen cache keys, worker request, useGeneratedCity key, store
  (persisted) + Density-panel sliders. Default = byte-identical (satellite
  draws appended post-harmonics). 3-centre render: satellite city with its
  own gridded core merged into the primary; buildings 9.3k→12.6k.
- **PS 5.1 mojibake incident**: bulk -replace via Get/Set-Content corrupted
  64 unicode chars in cityGen.ts (read as 1252); `git checkout` + Edit-tool
  redo; promoted to global memory (tools/powershell-51-utf8-roundtrip).
- gate1 PASS; buildings back to ~10.5k avg (packed frontage). Note:
  district CHARACTER still radiates from the primary centre only —
  satellites read residential/mixed; tying character to local density is the
  follow-up if satellite downtowns are wanted.
- **Radial patches dialled down** (user: "like a Star Wars death star"):
  7% odds (was 22%), 0.45× size (was 0.7×) — a two-ring circus block, not a
  ringed district. gate1 PASS.
- **"Population" building tint** (user): Debug View tint mode 6 — per-building
  people-equivalent (shared `buildingPopulation()` estimator), p95-normalised
  city-wide, packed as `aMisc.w` (vec3→vec4; a separate attribute would have
  hit the 16-slot vertex-attribute cap). Shader ramp matches the heat-map
  inferno so tint + ground overlay read as one scale. Prod build PASS.
- **Profile preview flow** (user): sliders edit a transient DRAFT
  (`densityProfileDraft`, never persisted); the heat-map overlay previews the
  draft's RADIAL field live (buildRadialDensity profileOverride — pure math,
  no regen, gen caches never see drafts) with white band-contour etching;
  reset/confirm buttons appear while drafting — confirm commits → ONE
  rebuild. Draft clears on group collapse. Needs a live eyeball (Playwright
  still down).

## Open for tomorrow

- **Hue UX reconsideration** (user flagged): wrap-around hues across 0°,
  third family / weighted palette stops, indicator style.
- **Daylight check** of the whisper defaults on other displays.
- exp² fog still broken (carried from 06-06 morning).
- #68 pitch multipliers, #69 archetype hover-highlight, #65 skybox sweep
  sync, #61 drag frame drops.

## Post-midnight round (06-08): settings polish, mixed-use blend, exurban tier, camera/intro fixes

- **Settings batch** (user): heat-map switch moved to the Density sub-group
  HEADER (mirrors shells); "centers" slider 1-6 (was 1-3, American English -
  visible-string sweep: color-coded, Wall/Fog color, colored, Gray; internal
  names stay as-is); satellites now FAN evenly (stratified sectors + jitter +
  seeded phase, 5 always-drawn slots) so 3+ centres never clump; profile
  defaults -> centers 4 / spread 1.2 / shoulder 0.8 / satellites 0.7; ALL
  panels + sub-groups collapsed by default; panel + gear get bg-popover/70 +
  backdrop-blur-md in EVERY theme (was grey-only); Roads header tri-switch
  lost its text label.
- **New boot defaults** (user): citySize tier 6 "City" (6 km) +
  fieldDeviation 1.5 (registry + state + module mirrors). Boot now pays the
  ~8-10 s gen (#63 mobile note left in comment). Saved configs override
  defaults at boot - Reset then Save to adopt.
- **Mixed-use blend** (user: "no spires but other large buildings, bias
  larger toward sides close to high/higher density"): own pickArchetype
  branch (single rng draw kept) - office-block + residential-tower share
  rides `urban`, a POSITIONAL gradient (radial density eased SUBURB_T->CORE_T
  max'd with silhouette coreProx) so the city-facing side of a district grows
  the big fabric; urban also counters the suburb shrink there. Verified:
  large share 10.1% -> 32.6% across the gradient, 0 spires, healthy 5-way mix.
- **Denser residential + exurban tier** (user): suburbAmount re-anchored
  piecewise ([CORE_T,0],[SUBURB_T,.45],[EXURB_T,.84],[RURAL_T,1] - was linear,
  0.64 at SUBURB_T) so the belt reads denser; NEW band "exurban"
  (EXURB_T=0.2) inherits the belt's OLD look; KEEP_ANCHORS gain the exurb
  step (belt 0.45->0.62/0.9); INTERIOR_DENSITY.residential 0.55->0.62.
  bandOf/plan overlay/heat contours updated. gate1 PASS; buildings ~+6%
  (19-21k); minor streets 3936->3368 (grid reaches further out, pods own
  less; band-stop now lands at density 0.42 vs 0.48).
- **Camera save fix** (user: "saved with default camera but doesn't stick"):
  cameraMode + orbitPaused flipped to persist:true (boot hydration made Save
  a WYSIWYG contract); save settles the LIVE azimuth into the orbit snapshot
  (store azimuth is stale mid-revolution) and rebuilds the intent from
  cameraLive when flying; CameraControls gained a mount-once fly spawn.
- **Intro waits for buildings** (user): IntroTicker takes cityReady - city
  cascade (windows + streetlights) starts on the false->true edge, boot AND
  every regen, clock held while the worker generates; stars still wake
  immediately so the sky isn't dead during the ~8-10 s gen.
- tsc / lint / gate1 / prod build all PASS. Live-eyeball list: blur themes,
  collapsed defaults, heat header switch, camera restore, intro timing on
  regen + tier switch.

## 06-08 settings IA pass 2 + intro/stars-on-regen

- **Debug tint moved to Buildings** (user): the tint group is now the last
  Buildings sub-group, renamed "Debug tint", on/off Switch on the header
  (new `buildingTint.enabled`; InstancedCity gates uDebugMode/uDebugTint on
  it - the retired "off" mode). Dropdown alphabetised + "off" removed
  (`BuildingTintMode` union dropped it), default mode = population, default
  enabled = false (boots untinted, dropdown pre-selects population). debug is
  persist:false so no migration. Search keywords moved Buildings<-Debug.
- **Roads -> expandable sub-groups** (user): Highlight (tri-switch on header,
  RoadHighlightTiers body), Streetlights (enable switch header), Traffic
  (enable switch header), Distance LOD (enable switch header). RoadsPanel
  refactor: exported RoadHighlightTiers/StreetlightControls/LodControls,
  removed RoadsSection/LodSection/HeaderRow/Subhead; section header lost its
  action.
- **Intro -> expandable sub-groups** (user): Windows / Stars / Streetlights
  are SubGroups (was SubHeader); SubHeader helper removed. Speed preset tabs
  stay at the top, ungrouped.
- **All groups collapsed by default**: top-level Accordion already boots
  closed (openSections=[]); every SubGroup defaultOpen=false. Removed the one
  remaining defaultOpen (Density, earlier).
- **Regen retriggers stars** (user): IntroTicker firstReady ref - stars still
  wake at mount (sky alive during gen), and now ALSO replay on every regen
  cityReady edge alongside the window cascade. First ready edge skips the
  star replay (already woke at mount).
- tsc / lint / prod build PASS. No gen change (gate1 unaffected). Live-eyeball:
  tint header switch + population default, roads/intro group expand/collapse,
  stars re-cascade on tier/profile change.

## 06-08 #49 Stage 4 — development clusters on the pods (wrap-up)

- **Node-proximity dev coupling** (plan-suburb-node-fields stage 4):
  buildDevelopmentMask takes an optional nodeProx field; keep probability in
  the suburban+ bands = keepProbForDensity x lerp(0.05,1,nodeProx), ramped in
  across the seam by suburbAmount (NODE_COUPLE_LO .15 -> HI .32) so the dense
  inner belt (no nodes) is flat and the core (p>=1, sub 0) is byte-identical.
  Cell HASH untouched (no new draws) - only the compared threshold moves.
- fillTensorBuildings re-samples the SAME node set the streets used
  (sampleSuburbNodes, same seed/radial/sketch-mask -> byte-identical list), so
  clustering lands exactly on the pod streets. Re-sampling (not threading from
  buildTensorRoads) keeps the worker path transfer-free.
- Effect: buildings ~19-21k -> ~15-17k; core stays uniformly packed, suburban
  belt shows pod structure, rural hamlets become distinct clusters with dark
  land between (samples/verify49/podClusterPng.ts top-down scatter confirms).
  gate1 + determinism PASS.
- **#49 disposition**: Stages 1,2,4 done; Stage 3 (connectors) superseded by
  Stage 2 connectivity-by-construction (anchor index + branch-snap + gap
  filler); Stage 5 (rural ring collector + variety polish) deferred as
  optional follow-up. The suburbs + density gradient deliverable is
  substantively complete - core->suburban->exurb->rural->fringe bands,
  loops-and-lollipops pods, mixed-use blend, multi-centre population field,
  and development clustering on pods all in.
- Needs live eyeball (Playwright down): pod clustering + rural hamlets in the
  3D scene at the 6 km default.

## 06-08 #49 CLOSED + shipped

- User call: Stages 3 + 5 done (3 by construction; 5 spacing+variety verified
  via seed sweep, rural ring intentionally dropped as a bullseye trap).
  plan-suburb-node-fields status COMPLETE; decision-density-gradient-model
  updated (5 bands, mixed-use blend, polycentric field, pod clustering).
- Follow-ups filed in the plan note (out of #49 scope): rural hamlet collector
  (organic, not a ring); ST_BAND_STOP tuning for more mid-band pod character.
- Merged feature/suburbs-v2 -> dev (--no-ff), dev -> main (--ff-only) + tag;
  GitHub #49 closed. Deploy source main updated.
