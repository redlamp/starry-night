# 2026-06-23

Working the 5-issue batch (#78, #65, #43, #53, #54) from yesterday's research, plus a
road-density debug viz.

## Status / decisions
- **#78 traffic** — fix committed on `feature/traffic-street-density` (fair MAX_CARS budget
  instead of order-truncation + minor carsPerM 0.005→0.008) but **UNSHIPPED/unmerged**. User
  still saw no street cars because they were testing `dev`/`main` without it. Adding a road
  traffic-**density** debug viz (population-heat colour theme) so the intended density is visible;
  targets below.
- **#65 skybox** — stars-vs-city sweep is acceptable (perspective fine, ortho ok). The real issue
  is the **moon is not aligned to the star skybox**: it renders in the MAIN scene (main camera)
  while the stars render in the StarPass (star camera at origin, quaternion-copied). Fix (greenlit):
  render the moon **in the star pass** so it shares the star camera and sweeps in lockstep with the
  stars. See [[moon-rendering]] §"Moon ↔ star render-pass alignment".
- **#43** — already mitigated (`app/layout.tsx` silencer); real fix upstream-gated (R3F 10 alpha).
  No action.
- **#53 device-adaptive quality** — mobile is now in scope (past v1) → greenlit. Delegated to a
  worktree agent. Plan: [[plan-device-adaptive-quality]].
- **#54 fog** — user repro: at **1693** from an OFF-CENTRE skyscraper cluster, the buildings are
  fully fogged. Root cause: `FogTicker` keys fog on camera→**CITY_CENTRE** distance, not
  camera→focal/target — so when the camera sits near centre but looks at a far off-centre cluster,
  fog calibrated to the (small) centre distance fogs the cluster out. Fixing the focal-distance
  bug first; broader extent-adaptation plan: [[plan-fog-extent-adaptation]].

## Traffic density targets (#78, at density 1, fully-busy segment)
| Tier | carsPerM × lanes | ≈ cars/km |
|---|---|---|
| Highway | 0.02 × 3 | ~60 (dense ribbon) |
| Arterial | 0.012 × 2 | ~24 |
| City street | 0.008 × 1 | ~8 (≈1 car / 125 m) |
| Suburban street | ×0.12–0.4 busyness | ~1–3 (trickle) |

Caveat: `MAX_CARS = 5000` + the new proportional scaling thins all tiers *together* at large city
tiers, so on-screen densities fall below these uncapped targets for big cities — the viz should show
the **expected (pre-cap)** per-segment density so the intended distribution is legible.

## Evening — feedback iteration (all on `dev`, unshipped; user tests tomorrow AM)

Consolidated the day's branches into `dev` (fog-focal #54, moon-lock-v3 #65, traffic-density-viz +
light-size #78) and added two more:
- **#78 viz** gamma-spread so the road hierarchy reads (highways yellow → arterials orange → city
  streets magenta → suburban purple), not flat indigo.
- **Car light-size slider** (Roads → Traffic → "light size") — street cars are the *smallest tier*
  (size 4); the fix newly populated streets so the small lights are *new*, not shrunk. Density ≠ size.
- **#65 moon v3 + v3.1**: the moon must render in the **star pass** to lock to the stars (a separate
  camera; that's why the main-scene v2 couldn't lock). v3 = star-pass + STAR_FOV scale-compensation
  + default elevation 1°→18° (clears the horizon). v3.1 = body drawn AFTER the stars (transparent +
  renderOrder 20, depthTest off) so **no stars show through the moon**, incl. the dark limb.
- **Camera low-angle direct-orbit**: below `rotateSlowBelowDeg`, blend the lever-arm azimuth toward a
  direct horizontal drag (right→left = CW). Justification: at low elevation the focal pin is pulled to
  the screen bottom, so the press-relative lever arm degenerates (long radius → wild azimuth on small
  drags). Sign one flip away if it reads backwards.
- **Fog default far 2 → 4.**

### Queue for tomorrow (diagnosed, NOT yet built)
- **Fog 1 — opacity cap:** THREE fog has no built-in max-opacity; add a custom fog-factor clamp
  (a `uMaxFog` uniform) so distant things never fully white-out. Also: confirm fog is keyed to the
  **focal-point distance** (it is, via #54) — the residual is **ortho**: in ortho the camera parks far,
  so `d` (camera→focal) is huge and fog/LOD stop responding to ortho zoom. Fix: use the orthoSize-
  *equivalent* distance (the `orbitFramingFactor`/`focalScrubScale` mapping) for `d` in ortho.
- **Fog 2 — fog the lights:** streetlights + beacons (red tower lights) are custom additive shaders
  with no fog. Add a distance-fade term (fade brightness toward 0 across the fog band) keyed to the
  same focal distance. ~per-shader uniform + a few lines each.
- **#78 car-light size:** size should depend on **camera distance only**, not the road tier — unify
  the per-tier base size (or drive size purely from distance LOD). And the **ortho** LOD bug above
  shrinks all cars (parked-far camera → LOD floor). Same ortho-distance fix applies.
- **Camera:** verify the low-angle direct-orbit feel; flip the sign if needed.
