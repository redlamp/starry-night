# 2026-07-03

## Fable session (morning) — supersample tempering + Window Lab

- **User verdict on yesterday's supersample fix**: parity confirmed with the
  annotated screenshot (their screen now matches the captures), but the result
  isn't acceptable — "lights look darker/muddied", and receding windows "grow
  to fill their full tile, so it doesn't look like windows anymore".
- **Tempered the two eager constants** (`1555ab6`, on `feat/view-links` since
  view-links stacks on supersample):
  - mask-mean feature guard 1–2 px → **0.5–1.1 px** (was swelling windows that
    were still ~7 px wide and plainly readable);
  - tap-average ramp span 0.3 → **0.35–0.9** (was dimming floors that read
    fully lit).
  - Gym scores land between baseline and the first cut (telephoto 235 vs
    366/122, band-close 250 vs 423/107) — deliberate trade of far-field
    suppression for mid-range window integrity. Live re-judgement pending.
- **Window Lab scaffolded** (`/window-lab`, branch `feat/window-lab`) — the
  user's ask: a bench to try NEW building/window constructions "untethered
  from the existing solutions".
  - Deterministic specimen rack reproducing the three #82 regimes: graze wall
    / mid cluster / far forest. Two slots render the same rack side by side
    (A at x=0, B at x=+900); approach registry = add a file + one entry.
  - Approach 1: **current shader** (production pipeline miniaturised, pinned
    to defaults). Approach 2: **baked facade + mips** — facades baked to
    ordinary textures, trilinear mips + anisotropic filtering do all the AA.
  - State is shareable via `?a=&b=&pose=`; camera presets per regime.
  - First finding already: the baked rack is confetti-free at distance but
    goes **grey/muddy** — linear mip averaging of warm windows against dark
    facade is physically right and perceptually wrong. Distant glow needs a
    deliberate tint (as the main shader's `distantGlow` does), whatever the
    construction. Bake fairness fixes applied (fractional band cut, TV
    time-average) so slot B doesn't overstate lit area.
  - Gotcha for lab pages: drei `CameraControls` ref attaches after parent
    effects run (R3F mounts Canvas children late) — pose presets need a
    state-ref, not a useRef.
- Branch state: `fable` ← `feat/window-supersample` ← `feat/view-links` ←
  `feat/window-lab` (stacked; merge in that order on signal).

## Fable session (afternoon) — hybrid port + the varying-ulp saga

- **Window Lab rounds 2–4**: Baked SDF (per-axis RG distances → exact square
  corners), then **Atlas + SDF** (1 texel/window + analytic field, ~340×
  memory cut) — the winner. TextureInspector + TextureHoverTip + per-slot
  texture-layer dropdowns. Lab tracker: [[window-lab]].
- **Hybrid mode shipped to production** (`feat/window-hybrid`): classic|hybrid
  toggle, `paneCoverage()` box-filter mask (kills the zoomed-in mullion
  stipple — the user's actual #82 complaint), `aMeanLit` far field. Ground-
  truth verified (supersampled-reference MSE, −23 % to −39 % every pose) after
  moireMetric scored the fix *worse* — sharpness-bias lesson recorded in
  [[window-lod-moire-diagnosis]] round 4.
- **"Major dithering" at a new far pose** → round 5: the varying-ulp family
  (hash amplification ×480 / binary-threshold flips / `mod` wrap at exact
  multiples). Three fixes in `cityInstanced`; rules codified in
  [[decision-shader-varying-precision]]. Notable detour: an instance-
  fingerprint probe near a byte-rounding edge faked a two-population depth
  fight; an angle-tolerant near-wall scan (2 pairs city-wide, both remote)
  corrected it. Fresh-context research agent contributed the depth-LSB math.
- **Shipped**: both batches committed, `feat/window-hybrid → fable → dev →
  main --ff-only`, tag `v2026.07.03`, Pages deploy green, user-confirmed live.
  Stack branches deleted. #82 resolution comment drafted (close pending
  user go-ahead).
- **Far-field LOD arc (evening)**: "orange columns" report → synthetic 4×3
  banding (rejected: patchwork) → design question "what does the LOD buy us?"
  (answer: temporal stability only, zero perf) → **mip-atlas far field**
  (lab approach-4's construction in production: trilinear twin of the packed
  atlas, continuous-uv sampling, tungsten-prefilled dark texels, deep-limit
  crossfade to `aMeanLit`) — real fenestration structure at range, no
  confetti. Then user direction: **window LOD off by default** — reserved
  headroom for a future complex near-field shader; `DEFAULT_WINDOW_AA
  .lodEnabled` flipped false (existing toggle in the LOD group re-enables;
  saved configs pin their own value).
- **R-reset short arc**: `resetToDefault` (StarryNightV2) rebuilt from
  `moveTo/rotateTo/dollyTo` with the home azimuth unwrapped to the nearest
  winding — `setLookAt` tweened numerically into atan2's (−π, π] window, so
  any pose across the seam revolved the long way (home azimuth sits at −178°,
  which is why it was common). CDP trajectory probe: 21.3° sweep (was 339°),
  0.00° end error. Live feel check pending. Probe gotchas: `?capture=1` gates
  the release-in-place grab (drive `setCameraHandoff` + `setCameraMode`
  directly); live pose reads from `cameraLive`/`orbit`, not `cameraIntent`.
- **Controls guide**: rows match on explicit `CameraAction` ids (labels are
  copy, not identifiers); double-click marks its own `zoomIn` so "Zoom In"
  lights instead of wheel "Zoom"; key rows highlight and R pulses `reset`
  until the transitions settle ("Reset Camera" lit for the whole flight).
  **Title Case** swept across all prompts + saved as a standing memory rule.
- **Radial intro wakes fixed**: streetlights + buildings' outside-in /
  inside-out modes fed `uIntroCityCenter` from `orbit.center*` — which
  writeOrbitPose tracks to the live camera aim — so the "centre" followed the
  camera. Now the `CITY_CENTER` world constant.
- **Shipped (evening)**: fable → dev → main, tag `v2026.07.03` moved to the
  day's last push. R-reset short arc, controls-guide highlights, radial-wake
  fix, mip-atlas far field, window LOD off by default.
