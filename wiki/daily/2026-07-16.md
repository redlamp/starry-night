# 2026-07-16

- Camera v3 round 7 (watchlist 6.5, closing the transport story): **Space toggles the
  idle drift** in v3 — OFF → enables auto-drift AND takes off immediately (backdates
  `lastInput` past any delay, so the ramped takeoff starts next frame); ON → disables,
  camera holds where the drift left it. Same state as the Orbit header transport and
  the Idle Drift switch; guide cheat-sheet + tooltips updated ("Space"). The Drift
  MODEL keeps its own Space=pause-revolution binding. CDP-verified both directions
  (takeoff within a frame at a 30 s delay; freeze on disable).
- Re-pointed the **v2026.07.15** tag at the day's final promotion (the round-6
  transport/default-off push went out untagged last night) — delete + recreate, per
  the one-tag-per-day / after-midnight rule; today's after-midnight round 7 rides
  under the same tag.
- Updated [[test-plan-2026-07-15-camera-v3]] (6.5 resolved) and
  [[decision-camera-v3-continuous-modes]] (transport paragraph).
- **Round 8 — drift transport rework** (user proposal review → adopted split): the one
  autoDrift flag became **drift MODE** (runtime `driftMode`; Space / floating
  helicopter button / "Drift" header switch / Orbit transport — flies until told
  otherwise, pauses for manual adjustments, eases back in ~1.6 s after release on a 5 s
  ramp) + **idle drift** (persisted `snv3.idleDrift` preference, off by default — takes
  off after Delay; interventions restart the full timer). Shared three-way transport
  `cameraCommand.toggleDrift` (stop-during-idle-flight does NOT flip the mode on). New
  floating button left of the settings gear: Rotate3d ↔ Helicopter, lit while flying,
  idle-fades with the chrome. Group "Idle Drift" → "Drift"; legacy `autoDrift` saves
  map to `idleDrift` in migration. CDP-verified: mode takeoff/pause/resume/hold, idle
  full-timer restart, space-during-idle stop, button icon swap.
- Round 8 polish: controls guide gets a live **Drift switch** (Space keycap) directly
  under the key block, bottom group ordered **Space, P, I, H, F**; the helicopter
  button now idle-fades even while lit; the **Diagram** toggle moved from Orbit's tail
  into the Camera section above "live view link" (it reads on the camera, not the
  orbit). Shipped to main as the round-8 promotion, tagged v2026.07.16.
