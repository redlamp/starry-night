# 2026-06-08

## Morning — /intro mobile fix, snow-globe default, README, EOL normalization

- **/intro 404 on GitHub Pages — FIXED + shipped.** The Mac GLB loaded from a
  bare `/models/...` path that 404s under the `/starry-night` base path
  (`assetPrefix` only rewrites Next's own chunks, not raw `useGLTF` fetches).
  Added `lib/basePath.ts` `asset()` helper; verified the deployed bundle requests
  `/starry-night/models/...` (200). Root cause: the intro merge shipped untested
  against the static-export base path.
- **Snow-globe is the /intro default** now (was flat screen). Shipped.
- **README + `docs/hero.png`** added (GitHub front page live); **`.gitattributes`**
  (`* text=auto eol=lf`) + local `core.autocrlf=false` to stop Obsidian's CRLF
  re-saves showing as phantom-modified `.md` files (renormalize was a no-op — the
  repo was already all-LF). Shipped.
- **Touch-to-focus for the CRT (mobile)** — tap the glass to focus, then drag
  orbits / pinch zooms / two-finger adjusts focus; double-tap recenters (screen
  cam on the glass, studio cam on Mac/stage). On `/dev` for device testing; not
  yet promoted to main.
- Tag `v2026.06.08` tracks the day's pushes.

## Camera-controls unification plan

Decided to unify `/` and `/intro` camera onto drei `<CameraControls>` rather than
keep patching the two-systems inconsistency (which is what surfaced the mobile CRT
gap). Forks settled: base = drei CameraControls (camera-controls); fly → drei
FlyControls/PointerLockControls; full scope (both `/` and intro). Auto-revolution,
projection switch, and north-up roll stay thin custom on top; phased behind the
Zustand store; `gate1` unaffected (camera isn't seed state).

- created [[plan-drei-camera-migration]]

### `/drei-lab` iteration + two locked decisions

Built out the lab controller and made two calls after feeling it on `/dev`:

- **Orbit = drei's model, wholesale.** Drop the bespoke 736-line orbit, including
  the focal-Y-only right-drag — right-drag becomes drei's native `TRUCK`.
- **Fly = desktop-only; mobile = orbit.** Verified in three-stdlib that none of
  drei's fly controllers move on touch (locomotion is keyboard-only).
  **Pinch-to-fly dropped**; mobile gets full touch parity via orbit instead.

Lab additions this session: `e.code` key tracking (fixes Shift), Q/C/Shift = down,
E/Space = up, **wheel = fly speed** (replaces Shift-sprint), drag-look via manual
clientX/Y deltas (so touch-look works), auto-orbit on `Space`, two fly→orbit exits
(home tween vs orbit-from-here), and `T` = top-down + fit-to-content. Real-app
fit-to-content framing deferred to [issue #79](https://github.com/redlamp/starry-night/issues/79).

- created [[camera-controls-feature-matrix]] — living old-vs-drei-vs-ours behavior
  table (canonical disposition for the migration; the plan's table moved here)
- created [[camera-tuning-notes]] — open tuning issues for the drei bridge (next up:
  **Focal Y = focal height** so the camera can look up; ortho parity; touch) + the
  done-this-session log. Branched the drei work to `feat/drei-camera-tuning`.
