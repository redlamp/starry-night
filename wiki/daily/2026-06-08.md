# 2026-06-08

## intro/exploration merged → dev → main (shipped)

Folded the `/intro` exploration into the main line and shipped it. (Earlier
06-08 post-midnight work — density blend, exurb tier, camera/intro fixes — is
logged at the tail of [[2026-06-07]].)

- **Merge** `intro/exploration` → `dev` (`--no-ff`, `a4eb8b2`), 25 commits,
  branch point `8ad35e4`. Then `dev` → `main` (`--ff-only`) + pushed — Vercel
  deploys `/intro` from main.
- **Conflicts (2)** — `IntroTicker.tsx`: reconciled dev's `cityReady` gating with
  the branch's `autoPlay` opt-out (both the mount star-wake and the cityReady
  cascade gate on `autoPlay`, default true; `/intro` passes `autoPlay={false}` and
  snaps itself awake). `wiki/daily/2026-06-07.md`: union. Follow-on: `ScreenCity`
  passes `cityReady` so badge replays advance the cascade clock. `decisions.md`
  auto-merged.
- **Verified** — `tsc --noEmit` clean, `bun run lint` (eslint) clean. Visual:
  `/intro` good; `/` good (star twinkle present but subtler than on the CRT).
  Prettier `format:check` is repo-wide debt (dev fails it too; not a CI gate) —
  left alone.
- **Settings collision check** — `/` and `/intro` share one `useSceneStore`
  singleton, but persistence is explicit-save only (the main-app Save writes
  `starry-night.savedConfig`; `revertToSaved()` hydrates at module init). `/intro`
  pins tier 3 in-memory and restores the visitor's tier on unmount; it has no Save
  button, so it can't write the saved config. No persistent collision — the stale
  IntroApp "closing the tab persists tier 3" comment was corrected.
- **Follow-ups filed** — #75 (port the intro star twinkle visibility to `/`),
  #76 (verify/tune twinkle + retrigger-on-regen compose on `/`), #77 (explore boot
  cascade timing on `/`). Earlier intro follow-ups still open: #71–#74.
- **Cleanup** — deleted `intro/exploration` (merged); removed the merge + intro
  worktrees (orphaned long-path dir cleared via robocopy mirror). `feature/suburbs-v2`
  (parallel session) untouched; `feat/suburbs-density` + `feature/road-reveal` kept
  (unmerged).
- See [[decision-intro-mac-viewport]]; merge plan in
  [[intro-exploration-merge-handoff]] (resolved).
