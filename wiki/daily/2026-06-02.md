# 2026-06-02

## Streetlight de-bunch (#46) + city gen off the mount-critical thread (#44)

Two near-term city-gen tasks, built in parallel background agents, validated, kept.

- **#46 — streetlights bunching on curved/radial roads.** Per-side min/max spacing
  guard in `emitRoadLights` (inner-curve compression) **plus** a global de-bunch
  pass: drop any lamp within `STREETLIGHT_MIN_DIST` (11.5 m) of an already-kept
  lamp, processed highway → arterial → local so main-road lamps win. 11.5 m is just
  under the grid's two-sides-of-a-street pair distance (~12–13 m), so the grid is
  untouched while denser cross-road convergence (radial hubs, concentric rings,
  intersections) collapses to match it. Measured: `NN < 10 m` went from thousands to
  **0**; pair spike (~6,240 lamps at 12–14 m) fully preserved; ~17 % fewer lamps,
  all of it convergence clutter. Determinism PASS. (User flagged the radial-hub case
  with a screenshot — diagnosed as concentric-ring + spoke convergence.)
- **#44 — first-load jank.** `lib/hooks/useGeneratedCity.ts`: warms the cold
  `generateCity` cache off the mount-critical path via `requestIdleCallback` (rAF +
  SSR fallbacks), flips `ready`; `Scene.tsx` + `PlanView.tsx` gate the city layers
  behind it. Canvas/sky/stars/moon/ground paint immediately; the city streams in one
  idle tick later (masked by the intro fade). Deferred, NOT a worker — the district
  field's `classify` is a closure (not structured-cloneable), and a worker risked the
  static export. Determinism untouched. (Static-export `bun run build` still to be
  verified at ship time.)

gate1 PASS, tsc + lint clean, Playwright smoke PASS throughout.

## Settings panel — Option D built then reverted

Built the manifest-driven Option-D panel + `/studio` comparison harness (see
[[settings-ia-evaluation]] Build log), reviewed live, **rejected**. Flat horizontal
chip drawer read poorly. Reverted on `arch`; code parked on `wip/settings-panel-d`.
Kept for v2: Basic/Advanced mode, search, clustering, pinned seed/reroll/intro.
Avoid: crowded horizontal top-level menu, lumped content. See the note's Verdict
section.

## Issues

- Filed **#48** — limit rooftop aviation beacons to the tallest building per local
  cluster (too many buildings beaconed). Deferred.
