# 2026-07-10

- Perf survey ran (internal pipeline profile + external best-practice research) on `feat/persona-perf`.
- Baseline measured: 2246 ms cold directory build at 39.4k personas (6 km tier), up from the stale ~500 ms/23.5k figure in [[decision-persona-architecture]].
- Created [[persona-gen-performance]].
- Perf round started on `feat/persona-perf`: deferred directory build (mount-gate fix + Skeleton) and lazy deep tier (per-persona flavor, per-building stories).
- Test plan renamed to [[test-plan-2026-07-10-personas]] (was `test-plan-2026-07-10`), per the `test-plan-[date]-[topic]` convention (user 2026-07-10); [[test-plan-2026-07-08-personas]] renamed to match.
- Perf round landed: cold build 2246 → 1376 ms; page mount pays zero persona work; card open ~6 ms first building. All gates green (typecheck/lint/personaCheck/cityGolden/principalCheck + boot capture). One-time population + relations re-roll, as flagged in the test plan §9. (Re-roll is attributes-only: household structure/counts identical — 17,189 hh / 39,413 personas at the default seed, same as round 10.)
- **District boundaries (option 1 from the blocky-outline discussion)**: boundaries were always street-formed (arterial-walled flood fill, ~7.5m raster) — the blockiness was the 80×80 overlay sampling. New `districtOutline.ts` traces loops at the raster's native resolution (region-left directed edges, left-turn rule at pinches, closed-loop Douglas-Peucker) — outlines now follow the arterials. Directory Districts header gained a Show Boundaries toggle (all districts outlined in their colours; hovered district fills at 20% alpha); toggle clears when the directory closes. Verified: 46/46 districts trace closed CCW outers with areas matching the field, ~68 pts/district, 116ms once. Option 2 (snap to arterial centerlines) and 3 (boundaries as street-loop data) stay parked.
- **Boundaries follow-up (same session)**: DistrictShells rebuilt on the traced loops (smooth fill per district + white traced seams + smooth hover highlight) — one district drawing style app-wide, shared geometry helpers in `lib/scene/districtOverlayGeometry.ts`. Settings > Districts gained a `boundaries` switch (same store flag as the directory toggle; flag no longer clears on directory close now that it has two homes). Directory pin pinned-state + Show Boundaries active state unified on `bg-primary/30` (the /15 default read too low for icon-only controls; replaced the pin's foreground plate).
- **Round 11 (user test-plan responses, [[test-plan-2026-07-10-personas]])**: §1-5 mostly checked good; changes from feedback — **1.6** building rows lead with address (fixed 4ch number column, space-padded) + Signature icon (hover = building name) left of the hh column; **1.7** district pin moved right of the header (left of chevron), pinned plate contrasts the THEME (light plate in dark mode) not the district luma; **2.1/2.4** district headers made hover-contiguous (no dead gap flashing back to the pinned district; leaving the list reverts to pinned); **2.2** MapPin marker at the pinned district's centroid in-scene (drei Html, keep-mounted anti-flash pattern); **3.2** focus's siblings always render (derived via shared parents — the cross-building family weave doesn't backfill sibling links; chart-level fix) + blood relatives get solid borders matching connector color; **3.3** constant border width, style/color-only changes (no px shift); **3.4** non-blood boxes dashed; **3.*** lucide gender icons (Mars/Venus/Transgender/NonBinary) in tree boxes (icon left, age right) and persona bios near pronouns; **4.1** staff roles right-aligned on both lines; **4.2** units as outline pills; **5** directory scrollbar padding matched to the cards; **7.*** businesses | Separator | residents in building cards; **7.5** idle staged generation — directory build restructured as a resumable generator, prewarmed in ~5ms rAF slices from ~3s after city-ready, then stories weave gradually (`DirectoryPrewarm`); sliced and sync paths share one generator so no work duplicates. **6.1** helicopters parked for a later session. **8.1** stale "#93 queued" bullet struck (shipped round 10).
- Surveyed light-sprite sizing across all five systems → [[light-sprite-sizing-survey]] (constant-size in perspective traced to per-system choices; fix shape sketched, not built). Landed option 2 street-snapped boundaries (worktree agent, reviewed), district pin restyled like BuildingPin, work/school address on persona cards.
- **Family tree stability (user)**: layout is now focus-independent — rows order by age desc (persona-id tie-break), partners sit on the age-implied side (replaces man-left/woman-right), the sibling row sorts the whole set (the focus was pinned beside the union — the actual "jumping"), Elsewhere footer gets a constant-height slot. Selection = highlight only.
- **Intentional re-roll (user: more multi-generational families)**: multigen weight 5→8, ~45% of multigen households keep both grandparents (married couple), family-weave gate 0.22→0.32 → multigen hh 4.87%→7.88%, residents with a grandparent 9.0%→12.9%, 41,613 personas (under the 42k cap). Batched the parked sfc32 swap into the same re-roll ([[persona-gen-performance]] priority 1 — DONE): persona layer moves to vendored xmur3+sfc32 (`seededRng`), story weave 739→395 ms; geometry/naming untouched.
- **Civic ID (user)**: per-resident records-office ID in a fictional AB-123456 format (nothing like SSN/state IDs); lazy flavour tier, mono line under the birthday on the persona card.
- **Family tree v6+ (user-driven session)**: descendant-breadth web (roots ±2 gens, full descent, aunts/uncles/cousins visible, 60-box trim), OKLCH lineage stripes + colorized connectors (midpoint blends), gender tints (green/orange/purple, 3rd prominence pass), focus = inset ring w/ shared bg, two-direction placement (children align under parents), double-shift connector bug root-caused (transforms batched after connector math), THREE view modes: Rows / Columns (axis-parameterized transpose) / bow-tie Fan ([[family-tree-chart-conventions]] §7 realized: FamilyFan.tsx, blood-only, wedge-click traversal). Resident card: unified StatRow (fixed icon slot), base type scale, fly-to-only row buttons (flyToBuilding/flyToSpan), industry/status/commute icons, alma-mater derivation (nearest-high-school rule, no re-roll), civic ID, work/school address rows.
