# 2026-07-06

Opus session, continuing from the 2026-07-05 inspect/focus ship. Two feature batches - SNCv2 WASD movement + hotkey remap, and Transport -> Flights live caps - shipped `feat/camera-wasd` -> dev -> main and deployed as `v2026.07.06` (`1c9ac4e`).

## Shipped -> live on `main`

### SNCv2 WASD fly-through + hotkey remap -> [[decision-snv2-wasd]]

- **WASD/QE** glide the rig over the city (horizontal heading; world-Y for Q/E). Speed scales with altitude (perspective) or `orthoSize` (ortho) so the glide feels constant at any zoom. **Move speed** slider in Settings -> Camera.
- **Hotkeys:** `F` = wireframe, `I` = inspect. Removed the stale bindings this collided with (`I -> Show Pin`, `F -> Fly`); Fly is selector-only now. Dropped the dead `toggleFly`.
- **Controls card:** WASDQE/R key rows + Wireframe/Inspect as toggles (keycap columns aligned), `Perspective / Ortho` label.
- **Drei WASD?** Assessed and declined - camera-controls has no WASD, and the Fly/FirstPerson controllers don't compose with the orbit rig or the faked ortho. Custom ~40-line integration instead.

### Transport -> Flights live caps -> [[decision-flights-live-caps]]

- **Planes** (0-12) + **Helis** (0-10) sliders - a size-zero cap on a fixed seed-baked pool (no regen, no shader recompile). Defaults **4 / 1**. Helis at 0 = helicopters off (replaced the enabled switch).
- Spawn row: Airliner / Cessna / Heli on one row under a "Spawn" header; dropped the corridor description.

### Convention

- Adopted `feat/<detail>` (not `feature/`) for non-Fable branches - CLAUDE.md + [[decision-fable-branch-model-scope]].

## Verification + deploy

- typecheck, lint, **gate1 PASS**, and the production build all green. WASD feel + the flights panel are Taylor's live test on the deployed site.
- Deploy succeeded first try (no per-SHA wedge this time). Non-blocking: the Pages actions warn on **Node 20 deprecation** (checkout / configure-pages / upload-artifact / deploy-pages) - bump when CI is next touched.

## Notes

- All code committed by explicit pathspec - a parallel session's uncommitted `wiki/projects/stage-1-streets-first.md` + `wiki/research/map-layout-references.md` were left untouched.
- created [[decision-snv2-wasd]], [[decision-flights-live-caps]]
