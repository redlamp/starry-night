# 2026-06-22

- Shipped star-twinkle controls to production (`v2026.06.22`): per-star twinkle depth + period-range sliders, selectable waveforms (sine/triangle/noise/flicker), base-ui NumberField steppers. Also reset-replays-wake (#81) and focal-marker cost fix (#80).
- Noise twinkle read as minimal → diagnosed: octave averaging regressed to the mean (variance killed) + value-noise rarely hits extremes. Patched with non-averaged detail octave + contrast gain (uncommitted pending review).
- Ran 3 parallel research agents on star twinkle (physics / shaders / white papers) → created [[star-twinkle-scintillation]]. Verdict: keep the per-vertex chassis, replace the signal (band-limited log-normal noise, `(sec z)³` amplitude, faster broadband spectrum, optional chromatic flash). Implementation plan still open.
- Built the scintillation rewrite on `feature/star-scintillation` (log-normal noise, airmass amplitude, chromatic flash, wave→noise collapse, size 36); **shipped to prod** (`v2026.06.22` moved to the merge).
- Moon Phase 0 + 1 on `feature/moon-phases`: locked to the star dome (anchored to world origin, not the orbit pivot) + stale-distance fix; lit Lommel-Seeliger sphere (no limb darkening) with date-driven phase (synodic, sampled once at mount), view-space `uSunDir`, earthshine, opposition boost, phase-scaled halo, and a phase scrub control (auto/manual). Still ahead: NASA albedo texture (the face), location/`χ−q` orientation, and the halo→phase fix below.
- **TODO (halo vs phase):** the moon halo is a full circular additive glow; on a crescent it wrongly haloes the dark limb too. The glow should emanate from the LIT portion (follow the bright limb / fade with the terminator), not encircle the whole disc.
- Ran 3 parallel research agents on the **moon** (astronomy / rendering / white papers) → created [[moon-rendering]]. Findings: the "not locked to starfield" bug = moon anchored to the orbit pivot (fix: one rotating sky group); phase from date is cheap + deterministic; render as a lit sphere with Lommel-Seeliger (no limb darkening) + NASA CGI albedo + earthshine. Also caught a stale-`distance` bug from the star-radius change (moon now floats beyond the star shell). Staged plan in the note.
