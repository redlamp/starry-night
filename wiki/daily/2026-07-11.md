# 2026-07-11

## Round 2 — test-plan feedback build (afternoon)

Morning testing checked off most of [[test-plan-2026-07-11-personas]]; the feedback batch
landed as one build (plan §7 "Round 2" lists the testable items). Three agents ran in
parallel (Sonnet ×2, Opus ×1) on non-overlapping files while the family-tree canvas was
done by hand; all diffs reviewed.

**Directory & settings**
- All remaining native `title=` tooltips (dock buttons, Inspect, camera "?", settings
  switches) → shadcn `IconTip`, Title Case action names.
- Settings > Districts hover unified onto `hoverDistrictId` → identical traced outline +
  20% fill as directory hover. `highlightDistrictId` lost its last setter;
  DistrictShells now reads the shared field (store field removal = follow-up).
- Build-progress ring (§1.*): `personaDirectoryBuildProgress()` in personas.ts (pure
  observation at the generator's yield sites, phase-weighted) + `DirectoryBuildRing` —
  a conic-gradient 2px ring tracing the directory panel border, rAF-polled, gone at 100%.

**Resident card (PersonaColumn/EntityColumns)**
- Header: persona titles `text-lg`, `née X` as secondary header text, selectable text,
  label/value grid (Age/Height/Build/Born/Time/ID), `~12 AM` birth times.
- Badges: `♐ Sag` 3-letter signs, Chinese = element+animal emoji only, all badges text-sm.
- Row order In City → Home → Commute → Work (Profession+Work) → Education → Relationship;
  current students fold school into a single blue School row (their education).
- Details + Family are collapsibles; open state shared across cards via sceneStore
  (`cardDetailsOpen`/`cardFamilyOpen`). whyAwake moved below Details+Family.
- Wrap rule everywhere (StatRow, family/offstage rows, ColumnStat): flex-wrap keeps the
  value's max-content base size so it drops WHOLE to a right-aligned second line instead
  of word-wrapping beside the label.
- Fly-to tooltips instant (`IconTip delay={0}`).

**Campuses (§3.5)**
- Two real campuses sited after the K-12 pass on a fresh `::personas::campuses` stream:
  the city's university + college names become physical school businesses
  (`schoolTier: "college" | "university"`); 66 adult students enrolled nearest-campus
  with commutes. Alma mater now = highest education: degree-holders link a campus
  (educationDetail name match, else tier/tenure rules), HS-only keep nearest high school.
- Determinism gotcha caught in verification: campuses entering the staffing weave's kind
  pool shifted two pre-existing K-12 staff counts — fixed by excluding campuses from that
  pool (campuses carry 0 staff for now; faculty hiring = follow-up). After the fix,
  pre-existing structural output is byte-identical; personaCheck 17/17, cityGolden PASS.

**Family tree — infinite canvas (§4.2)**
- ScrollArea shell replaced by a gesture viewport: drag pan (4px threshold, pointer
  capture only once a drag is real so taps still click boxes), wheel zoom anchored under
  the cursor, two-pointer pinch (midpoint-anchored zoom+pan in one gesture), release
  snaps the nearest generation column flush (≤40px magnetic), double-click re-fits,
  scale clamps [fit×0.5, 2.5], pan clamps to keep 48px of chart on screen.
- The card still grows to fit small trees exactly (explicitly-sized viewport, `my-auto`
  keeps the footer pinned); when the panel caps shrink it, the automatic fit scales the
  whole tree into view — the scaling the user OK'd, replacing trim-or-scroll. The
  view-aware `maxPerGeneration` trim is gone; only familyWeb's global box cap trims.
- Measure pass now also clears the gesture transform before reading rects (a scale would
  corrupt the pack shifts), captures per-generation snap targets, and restores the user's
  view (or the fit) after layout. No scroll containers exist → the transform-ghost
  scrollbar class of bug is structurally impossible.
- Open Full Card icon → `SquareArrowOutUpRight` (it already pushed the persona into the
  columns and closed the dialog).

## Round 3 — live-feedback tweaks (same afternoon)

- Minors show no Relationship row (omitted, not "Child" — romantic status doesn't apply to
  children and their family role already reads in the Family section; row survives if data
  ever paired a minor).
- Family header: the tree trigger moved INSIDE the CollapsibleTrigger (span-rendered
  Button, the directory-pin pattern) so it sits immediately left of the chevron, chevron
  far right. Gotcha: BOTH the `DialogTrigger` and the `Button` need `nativeButton={false}`
  — Base UI console-errors if either still expects a native button.
- Astrology badges: sign glyphs at emoji presentation (`symbol + U+FE0F`) and text-base
  size, 2-LETTER sign abbreviations (`♎️ Li`); Chinese badge emojis larger. Hover cards at
  text-base / w-72 with the big three (Sun/Moon/Rising) each carrying their sign emoji;
  the element color-dot replaced by the element emoji.
- Header details re-cut three times on live feedback, FINAL: a two-column grid
  (1.25fr/1fr), each cell label-left/value-right + nowrap: Gender|Age, DOB|Time,
  Height|T-Shirt, ID|In City. In City moved OUT of the Details rows into the header.
- Zodiac hover card: big three marked ☀️/🌙/🌅 + sign emoji, epithet on the heading
  ("· the inner weather"), and a NEW per-sign one-liner below each — MOON_SIGN_TRAITS +
  RISING_SIGN_TRAITS added to personaData (same wry register as the sun traits).

## Round 4 — fly-to framing + astro polish (evening)

- **Obstruction-aware fly-tos**: `focusRequest` gained `insetLeft` (px of viewport covered
  by left-docked UI). Fly-to helpers measure `[data-camera-obstruction]` elements (columns
  row + directory panel, clamped to 60% of the viewport) and pass it; StarryNightV2Model
  fits the sphere into the REMAINING width (hFov scaled by `avail`) and biases the target
  to that region's center via `setFocalOffset(-insetWorld/2, 0, 0)` (negative x = camera
  left = subject right). Ortho ramp divides by `avail` too. Closing the columns clears the
  offset. CDP-verified: pin lands at the free region's center (1137 vs 1124 expected).
- Zodiac hover: sub heads ("the inner weather") on their own line under each big-three
  heading. Chinese hover restructured: header → born year → animal-emoji + year
  description → element-emoji + element description (names not repeated).
- Exact birth times (`8:34 AM`): `birthMinute` drawn at the flavour stream's TAIL —
  appending after civicId leaves every earlier draw untouched (the "keep new draws below
  this line" comment now marks the tail).

## Round 5 — live test-plan feedback (evening)

- **Tree fills the panel**: the chart viewport is now `absolute inset-0` (an invisible
  in-flow SIZER still drives the panel's grow-to-fit sizing); header + footer/controls
  hover OVER the chart on gradient scrims (`pointer-events-none` containers, `auto` on
  the controls). Fit math clears the overlays (56/72px pads); pans can travel under them.
- **Crisp zoom**: removed `will-change-transform` from the gesture layer — it pinned the
  composited raster at layout scale, so zoom scaled PIXELS of vector content (the
  "pixelated SVG" report). Browsers re-rasterize crisp after the gesture without it.
- **Desktop zoom demoted**: plain wheel PANS (shift = horizontal), Ctrl/Cmd+wheel zooms —
  which is also exactly what trackpad pinch reports as. Touch pinch + dblclick-refit
  unchanged.
- **Work/Profession merged**: one green Work row (money) — profession-category icon,
  title, then employer/district/address lines; Home is orange (hearth) `#f2a24a`,
  Work `#3fa87e`. Education keeps blue.
- **DOB cell** spans the grid and includes the exact time (`Oct 19, 1980 · 7:31 PM`) —
  answered: date + birth time are separate seeded draws (no Date object), composed at
  display time.
- **née → born**: header reads `Nicole Ortiz born Park` (plain-English genealogy form).
- **Camera free-area centering, fixed properly**: obstruction is now measured by the
  MODEL at consume time, one rAF after the request — so address-link focuses that push a
  new column measure the row's REAL width including that card, and every focus path
  (focusBuilding, fly-tos, cone, district pin) centers on
  `inset + (viewport - inset)/2`. `insetLeft` plumbing removed from the request type.
- **R2 fixes**: progress ring moved to the City Directory DOCK BUTTON (round conic ring +
  faint base track; panel ring kept); Show/Hide Settings buttons got shadcn tooltips;
  district hover = THICK outline (Line2 fat lines, 3px screen-space — LineBasicMaterial
  caps at 1px on Windows; CommuteArc's resolution-sync pattern) + translucent full-color
  fill (~22%) ALWAYS on hover, no longer gated behind Show Boundaries.

## Round 6 — R3 follow-ups, day's last build

- Tree: "?" HelpHint in the footer (nav cheat-sheet), double-click reset tweens home
  (280ms ease-out cubic, cancelled by any new gesture, cleaned up on unmount).
- DOB | Time back to two grid fields (user liked the separation; the question was about
  storage — kept as seeded numbers, a real Date would drag TZ semantics into the fiction).
- Maiden-name overflow plan: name+maiden > 24 chars → `(Park)` instead of `born Park`.
- Selection-follow: new `flyToEntity` in EntityColumns — every top-of-drill change glides
  the camera (persona→home, company/building→site, street/district→showLocations);
  cone-follow supersedes. Verified live: pushing a partner's card glides to his home,
  centered in the free area.
- District hover fill moved UNDER the buildings (depthTest on, renderOrder 0) at 40% of
  the border colour; the thick border stays a depth-ignoring overlay above everything.

## Round 7 — final polish

- Tree "?" is now a TOGGLE (icon-toggle convention) revealing a horizontal nav strip
  flush to the panel bottom — the main ControlsGuide's amber kbd-chip anatomy
  (Drag/Scroll Pan · Shift+Scroll Sideways · Ctrl+Scroll / Pinch Zoom · 2×Click Reset ·
  Click Re-root).
- District hover fill: the depth-tested under-buildings version didn't read on the
  user's machine — reverted to the stated fallback, an overlay fill in the border's
  colour at 40%.
- Family-section question answered with data: 3,045 residents (8.3%, all adults) have
  neither woven in-town links nor offstage relatives → no Family section. Not a
  regression (weave untouched today; personaCheck counts identical); the whyAwake
  reorder just makes the absence read more plainly. Possible future filler: a muted
  "no family in town" line.

**Verification** — typecheck + lint clean; personaCheck 17/17, cityGolden PASS,
districtOutlineCheck PASS. Playwright's transports still hang on this box, so the live
checks ran over raw CDP (cdpShot.ts pattern): Amanda tree = no scrollable elements, zoom/
pan/dblclick transforms exact, drag never changes selection; small-viewport run proved
the capped fit (scale 0.637) and caught the column snap landing at its exact target
(`12 − 12·s`); campuses searchable ("Havenwood State University", "Morris College"); a
degree-holder's card linked the university with the duplicate institution line suppressed.
