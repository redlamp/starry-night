# 2026-06-05

## Meteors debugged + rebuilt (#26 slice 2 follow-up), shipped to /dev

- **Invisible-meteor hunt, root causes in order**: (1) `active` is a GLSL
  reserved word — the persistent unnamed `VALIDATE_STATUS` error; naming every
  ShaderMaterial (`mat.name`) is what finally surfaced it. (2) Sub-pixel
  sprites — star-field's `300/d` distance attenuation at dome radius 9000
  crushed a 40px base to ~2.8px; head now rides `stars.factor × 4`
  (planet-scale, ~10–13px on screen). (3) Spawn band — diagnostic magenta ring
  at alt 10° proved the render path and pinned the visible sky band; streaks
  now fly a low lateral arc (alt 4–16°, shallow descent).
- **Camera-anchored spawn**: each fire samples the main camera's view azimuth
  → spawn wedge = star-camera horizontal half-fov minus margin. Every streak
  lands on screen in either projection (star pass copies only the
  quaternion); world-fixed during its ~1s life, no orbit drag.
- **Gap model rebuilt per spec**: dual-handle shadcn range slider (min/max
  seconds, editable value steppers, switch between label and slider). Each
  fired streak rolls the NEXT gap uniformly in [min, max] off a seeded rng
  chain (`deriveSeed(masterSeed, "meteors")`) — CPU scheduler pushes
  `uFireTime`/`uFireSeed`, shader stays closed-form. Bucket-hash law dropped:
  it produced triangular gap distribution, not uniform. Defaults **2–40s**;
  star size default 36 → **50**.
- **`scripts/shaderCheck.ts`** (new gate): headless GLSL parse via
  `@shaderfrog/glsl-parser` over all 6 sky shaders — the browser-free stand-in
  for shader compile errors while Playwright stays broken on this box. Exit
  code was inverted on first landing; fixed.
- **#61 filed**: frame drops while dragging the camera. Suspects: #55
  per-frame tile-cull compaction churning during continuous frustum change,
  star fill-rate (factor 36→50), HUD re-renders. Verification recipe in the
  issue.
- **Workflow**: back to `feature/*` branches off dev per repo rules — tonight
  shipped via `feature/meteors`, `--no-ff` into dev.
- Pushed dev: sky slice 2 (`582a6ad`), wiki (`461bdfc`), meteors. **Pixel 6
  pass on #55 still pending.** #26 remainder: moon-phase model → full-moon
  star-cut + true 21.84° halo ring.
- **#62 filed**: deploy-pages action on deprecated Node 20 — forced Node 24
  default 2026-06-16; bump action or opt in early.

## Morning — git hygiene, #62, merge-style decision, mobile dev access

- **Hygiene pass**: deleted `arch/city-planning` (merged), 2 agent leftovers,
  `feat/highway-crossings` + `chore/remove-git-gate` + `wip/settings-panel-d`
  (user-approved; superseded), both spike worktrees. Kept
  `feat/suburbs-density` (#49). Remote `wip/settings-panel-d` deleted;
  `origin/arch/city-planning` pending (classifier).
- **#62 closed**: `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24` on the Pages workflow
  (727a35e via `feature/ci-node24`); deploy run verified all actions on Node
  24. dev's stale workflow copy synced (be5757f) — it predated 3fa465d and
  would have regressed the live deploy.
- **Back-merge + merge styles**: settled the three direct-to-main CI commits
  into dev ancestry (5b10706); adopted feature → dev `--no-ff`, dev → main
  `--ff-only` + tag, direct-to-main = CI/hotfix only with same-session
  back-merge. created [[decision-merge-styles]]
- **Mobile dev access**: phone couldn't hydrate local dev — Next 16
  `allowedDevOrigins` (LAN origin blocked; promoted to global memory) + the
  printed "Network:" IP was the NordLynx adapter, real one 192.168.178.120.
  Mobile perf confirmed fine; TEMP eruda console removed from
  `app/layout.tsx`, `allowedDevOrigins` kept.
- **#61 next**: drag-perf diagnostic (FPS drag-vs-static → LOD tiles toggle →
  stars count).

## Afternoon — promotion shipped, #61 probed, road-reveal designed

- **Shipped to main**: merge-styles docs + LAN dev origins promoted
  `727a35e..578e649` (`--ff-only`, first under new style), tagged
  `v2026.06.05`, Pages deploy verified green.
- **#61 partial**: built `scripts/dragPerf.ts` (headless tile-cull drag
  harness). Default City tier = **0% churn** while orbit-dragging (frustum
  swallows the whole 3 km city) — #55 compaction never fires; metro = 86%
  churn but CPU-trivial (0.19 ms/f). Re-ranked suspects: per-pointermove
  `setOrbit` → whole-`orbit` React subscribers (CameraPanel/CameraControls/
  Moon). Phone fine (120 fps touch boost = display refresh). Desktop HITL
  probes still pending; sidebar-scroll dip was a slider-adjust artifact.
- **Road reveal designed** (visual-companion mockups): centre-out cascade
  picked, trace-softens wait, every-load replay, roads-lead-buildings-follow,
  4 s + slider. created [[decision-road-reveal-cascade]]; spec at
  `docs/specs/2026-06-05-road-reveal-cascade.md`.
- **Road-gen perf**: roads phase ~6 s of ~7.4 s at metro (worker keeps fps
  high; wall-clock is the issue) — filed as #63.

## Evening — road-reveal cascade implemented (feature/road-reveal)

- Subagent-driven execution of the plan: 13 commits, every task two-stage
  reviewed. Review catches that mattered: closed-ring normalization
  (revealAt > 1 opposite the attach), warm-cache reset race (effect vs
  useFrame → in-render adjust), ticker mount order (intro gate read the
  singleton's initial 1 before the clock's first write), GenTrace stale-ref
  bleed on tier switch (unconditional mount removed the remount self-clean).
- Headless gates all green: lint, tsc, build, revealSanity 9/9,
  shaderCheck 10/10, gate1, cityGolden byte-identical.
- Final-review notes for the record: closed-ring reveal path is currently
  unreachable (gen pipeline drops closed highways before
  `city.topology.highways` — defensive code, sanity-covered, never visually
  exercised); traffic is gated via the intro timeline (transitively ≥ 35%
  cascade), not literally "the same gate" as spec §5 wording; schedule build
  is ~635 ms main-thread at Metro right at cascade start — perf follow-up
  issue to file. HITL visual gate pending.
- **Visual-gate tuning round** (uncommitted with the note): straggler tail
  bunched (`TAIL_EASE` 0.7), tips retire by 92%, tips 2× then re-scaled, tail
  ⅓ length, stagger window halved (`RADIAL_SPEED` 2000), and a new **scout
  glint** phase — a point of light races the highway path before the cascade
  (`SCOUT_PHASE` 0.15). created [[road-reveal-choreography]] (phases +
  glossary + tuning quick-ref); un-stubbed [[architecture]] MOC.
