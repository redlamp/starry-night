# 2026-07-02

Starry Night Cam v2 — brought Skyline Mode to the perspective camera, then a pass of interaction
affordances (cursor + glyphs + controls guide). Detail in the decision note; this is the arc.

## Perspective Skyline reframe — [[decision-perspective-skyline-reframe]]
- The long-running "low / flat perspective" problem resolved. Root cause named: at a flat aim the
  orbit rig couples `eye.y == target.y`, so you can't drop the camera without dropping the focal.
- Landed split: **LMB** rotates / tilts around a fixed pin (synthesised mid-map pivot when flat),
  drag-up flattens + descends, floor **1 m** (was 5 m); **RMB up/down** pedestals the **coupled**
  eye + focal (altitude, floored 1 m); **RMB left/right** trucks. Tilt clamped `≥ 0`.
- Ortho unchanged — keeps its focal-offset lens-shift reframe (now gated to full ortho).
- Rejected en route: auto-descent (ground pull), GE re-centre, pure lens shift. See the note.

## Interaction affordances
- **Deferred pin**: the orbit pivot pin is captured on press but only shown once the drag crosses a
  ~3 px threshold — a click / double-click-to-zoom no longer flashes it.
- **Cursor state machine**: `pointer` (idle) → `grab` (Shift or Ctrl/⌘ armed) → `grabbing` (only
  once a drag actually moves, not on bare press).
- **Modifier glyphs**: while over the canvas, Shift shows the move icon and Ctrl/⌘ the eye icon,
  riding the cursor — mirrors the mouse-down options and flows straight into the drag.

## Controls guide
- **Look Around** row now shows the LMB+RMB chord glyph, sub "or Ctrl + LMB".
- Right-aligned scene-affordance icons per row: blue pin (Rotate & Tilt), move arrows (Move), eye
  (Look Around) — same sky-blue the scene renders them in.
- Added **Ortho / Perspective (P)** toggle and **R · Reset camera** to the v2 sheet.

## Verification / open
- tsc · ESLint · Prettier · `bun run build` all green.
- Feel rests on the live session ([[feedback_interaction-feel-verification]]) — synthetic / CDP drag
  measures geometry, not feel.
- created [[decision-perspective-skyline-reframe]]

---

## Fable session (afternoon — first Fable 5 session on the project)

- Ran the 5-agent codebase survey → [[fable-codebase-survey-2026-07-02]]:
  B+ health, determinism contract verified end-to-end, ranked backlog.
- Git-history review: hygiene strong; flagged `feature/road-reveal` (15 unmerged
  commits), stale `feat/drei-camera-tuning`, no CI gate. Git items parked.
- Created the `fable` branch off dev — all Fable work lands there.
- **Window moiré diagnosed + fixed** → [[window-lod-moire-diagnosis]]. Root
  cause: lodNear default regression (0.2→0.4 in yesterday's LOD tuning) +
  band-pane brightness jitter aliasing under ~4 px panes. Fix `ee9e79f`,
  verified by capture metric on the RTX 3080 Ti. Awaiting live confirmation
  at the original pose.
- Worked the survey backlog on `fable`: shadcn→devDeps + missing typecheck
  script, `randomSeedForReroll` guard-rail, security headers, uniform
  write-gating in InstancedCity (agent draft had 2 real bugs, caught in
  review), lib→components layering fix, wiki retag + orphan links, PRD status
  refresh, tile-cull profiling (1.33% recompaction in drift orbit → no
  hysteresis warranted). sceneStore + CameraPanel splits in flight via agents.
- Survey's Traffic `.slice()` item closed **invalid** — the copies are
  load-bearing for tile compaction.
- GH Pages deploy: 5 attempts timed out/cancelled server-side; deployments are
  keyed by commit SHA and main@52e7782's record is wedged at GitHub. Unwedge =
  any new SHA on main; rides along with the fable promotion.
- Moiré round 2: telephoto pose (2.77 km, fov 24.6) has inherent sub-pixel
  window aliasing — diagnosis + candidate fixes filed as #82; z-fighting and
  placement overlaps ruled out with data. Footer icon actions + name-only
  tooltips shipped per live review.
- created [[window-lod-moire-diagnosis]]

## Fable session (evening) — issue triage + low-angle camera diagnosis

- GitHub issue triage: closed #65 (skybox sync — fixed 2026-06-24 by `3d8745e`,
  never closed) and #54 (fog extent — superseded by the 2026-07-01 world-absolute
  fog revert + fog default-off). Plans/status left on #61, #69, #53, #43, #2.
  #53 decision recorded: adaptive quality stays default-off (already was).
- Fixed the DPR select trigger label ("2" → "2×", `1a10f08`) and the stale
  camera-relative fog comment in sceneStore (`3450d6d`).
- **Low-angle "ground pull" sightings diagnosed** (user report, v2 camera). The
  named `groundFraming`/`applyScreenFocus` pull never runs in v2 (map/drift/
  turntable only). Three v2 mechanisms produce pull-like motion at low angles:
  1. **Zoom pivot unclamped** — grazing cursor → ground hit tens of km off-map,
     wheel/dblclick lunges the camera. Orbit + pan clamp to the disc; zoom
     didn't. Fixed `c7f2ad4` (clamp to city disc). Live feel test pending.
  2. LMB tilt about a far pivot (horizon click clamps to the disc EDGE, km
     away) — descent rate scales with pivot distance; the descent itself is
     the adopted coupled-rig design ([[decision-perspective-skyline-reframe]]).
  3. Ortho Skyline per-frame ease re-applies the remembered RMB rest framing
     when the aim crosses the 2° threshold — an actual auto frame slide.
  Items 2–3 are design calls, surfaced to the user before touching.
