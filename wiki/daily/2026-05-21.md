## Day 1 — PRD review

- Reviewed `docs/PRD.md`, identified gaps: perf budget, rendering strategy, derived-vs-runtime state confusion, flicker determinism, aspect handling, color pipeline, agents
- Resolved 12 open points with user; updated PRD §4, §5, §6, §7, §8; added §9 (agents) and §10 (docs conventions)
- Created [[decision-prd-v1-architecture]] capturing decisions reached today
- Wiki bootstrapped: `index.md`, `mocs/decisions.md`, this daily log

### Decisions made

- Bun + Next.js + R3F (pnpm fallback)
- GSAP dropped from v1, reserved for vignettes later
- shadcn/ui deferred to M4
- Windows shader-painted on building faces (not geometry) — scaling decision
- `InstancedMesh` per archetype, per-instance attrs for height / seed / district
- Zustand only for runtime state; derived state recomputed not stored
- Deterministic flicker via shader `(windowSeed, uTime)` math
- Aspect-bucket camera (landscape / square / portrait)
- Seed in URL hash
- sRGB output + ACES tone mapping
- ESLint + Prettier baseline
- Still-frame review via `bun run capture` script

### Open

- Visual style commitment — defer until still-frame review
- Sky color final treatment — defer
- Snapshot diff agent — possible M2+ tool

### Next

- Scaffold Next.js + Bun + R3F (M1 ticket 1)
- One placeholder building under starry sky

### Session ran past midnight

Work continued into [[2026-05-22]] — M1 tickets 2 and 3 plus camera + UI tooling landed in the same dev session.
