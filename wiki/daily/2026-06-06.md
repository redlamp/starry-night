# 2026-06-06

## Road-reveal parked; choreography editor preserved

- **The pivot**: after a tuning round (scout glint act, tail bunching, tip
  retirement, halved stagger) the verdict landed â€” the multi-act reveal is
  *too theatrical for an ambient screensaver homage*. Wrong project for the
  animation, not a quality problem. Parked deliberately at full quality.
- **Branch â†’ draft PR #64** (`feature/road-reveal`, 16 commits, all gates
  green, two-stage reviewed). Draft so it can't merge by accident; PR body
  carries the port-back checklist (prototype timing model, radial GenTrace
  births, Metro schedule-build perf, closed-ring + traffic-gate notes).
  Branch survives hygiene passes while the PR is open.
- **Choreography editor â†’ dev** (cherry-picked `5ce764d`):
  `docs/prototypes/road-reveal-choreography.html`. What started as a phase
  mockup became a project-agnostic motion-design tool â€” GSAP timeline +
  GSDevTools transport, layered actor tracks (drag to move, edge-drag to
  resize, draggable gate line), per-actor ease menus including the `in`
  family, shadcn-styled cards with stepper number-fields, live canvas, cast
  readout. Staged in global memory (`domain/`) as a promotion candidate
  (plugin/skill or standalone repo).
- **Wiki**: [[decision-road-reveal-cascade]] â†’ `status/deferred` with park
  addendum; [[road-reveal-choreography]] header notes the park + editor
  capabilities; [[architecture]] MOC links both.
- **Carried context**: #61 desktop HITL probes still pending; #63 (road-gen
  wall-clock) still open and independent of the park; the reveal-specific
  Metro schedule cost lives only in the PR checklist now (no separate issue â€”
  parked code doesn't get perf issues).

## Evening â€” correlated window lighting (dev)

- **Windows height ranges** (`a5025ee`): height joins width as a per-building
  [min, max] roll in both window modes; advanced drops the hard-coded Â±15%
  jitter; RangeSlider bounds editable; old saves migrate Â±0.08.
- **Correlated floor lighting** (`0d69758`): mode taxonomy v2 (per-window /
  per-block / fractional-floor / whole-floor 35/25/30/10 for offices,
  warehouses 40% fractional). Band rows tagged atlas alpha 200; per-face
  uneven segments; switch-bank stagger slider; glass fraction to 1.0 with
  phantom-mullion fade. Advanced panel: archetype icon filter bar + base-ui
  tooltip component. See [[decision-correlated-window-lighting]].
- **Curtain wall share** (follow-up commit): `uCurtainShare` (default 0.3) pins a
  seeded share of correlated office towers to full-bleed glass â€” live slider,
  no atlas rebuild. Next: user testing; pitch multipliers (floor height /
  column width gen inputs) remain optional follow-up.
