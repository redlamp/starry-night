# 2026-09-05

## Project review (Fable 5.1)

First Fable 5.1 session. Four Sonnet audits (PRD vs code, labs and scripts,
wiki staleness, gates and health) plus two more (architecture decomposition,
runtime performance), reconciled into a review page:
`docs/reports/review-2026-09-05-road-to-2.0.html`, published as a claude.ai
artifact with shared marks and notes (the page's `db` store, doc
`review/decisions`).

Verdict: call it 1.8. v1 shipped and was exceeded; a second product landed
June to July with no spec above decision notes; M2 window dynamics still
"not user-facing"; the Andy playtest's tier 2/3 findings were synthesized and
dropped; no CI gate beyond deploy; wiki index badly stale.

Measured (dev at bc107eb, metro tier): generateCity 790 to 870 ms, window
atlas 200 ms uncached, names 232 ms, persona directory 341 ms headless,
16 draw calls, 9.3 MB of window atlases (36% packing waste), 3.11 MB first
load on `/` of which 732 KB is recharts for the demographics panel. About
15,900 buildings, not the 1,500 the PRD annotation claims.

## Decisions from Taylor's read-through

- Stay in 1.x releases for now; 2.0 is the plan, not the next tag.
- The PRD is a stale instrument. Replace, don't rewrite: a one-page charter
  in docs, a product MOC in the wiki linking decision notes as the living
  spec, and a GitHub milestone for the 2.0 gates.
- Modern lighting greenlit as a lab experiment: [[plan-night-shift-lighting]]
  (perpetual night, so the mode is about who is awake, not what time it is).
  Venue: `/window-lab` as approach 5. Do not delete the window lab.
- Portraits: hold the generative system (#100); a pictogram stub (man /
  woman / child on a family-hued tile) ships in the persona card now.
- Retire the V2 camera model; keep the registry so models stay swappable.
- Mobile default: mid tier on coarse pointers (#85).
- Freeze `fable` branch routing as history; feat/\* off dev (decision note
  still to write).
- Tests: wrap the gate scripts as `bun run test`, wire CI. No vitest.
- Branches: archive-tag `feature/road-reveal`, delete `feat/figma` and the
  worktree branches.
- Day/night cycle and Supabase: confirmed dropped.
- Interested, not scheduled: billboards/signage (like storefronts), in-city
  camera positions, traffic lights (needs traffic v3: routed cars instead of
  per-road loops, which also fixes the rural looping), return to `/intro`.

## Code

- `components/ui/columns/PersonaPortrait.tsx` (new) wired into the
  `PersonaColumn` pinned header. Lint/typecheck pass; verified in the live
  card via `?probe=1` + `setDirectoryOpen(true)`.
- `.claude/launch.json` (new, untracked) for the dev server preview.
- Nothing committed.

- created [[plan-night-shift-lighting]]

## Bundle investigation (worktree agent)

Chased a reported per-route three.js duplication (~530 KB copies across `/`,
`/intro`, `/camera-lab`). On this branch's post-camera-v2-retirement `dev` tip
it no longer reproduces - `bun run build` shows Turbopack already sharing one
instance of three's core chunks across every route that touches it. No config
change made; wrote up the method and findings in
[[decision-shared-vendor-chunk]] so the next report of this can be checked
fast instead of re-diagnosed from scratch.

## Afternoon: delegated sweep and the first perf and design rounds

Merged to dev (all `--no-ff`, pushed; CI green from 2582b8b once the golden
was skipped in CI):

- feat/road-to-2.0: review page, charter, wiki sweep, decision notes.
- feat/persona-portrait-stub: pictogram portrait on the resident card.
- feat/infra-gates: `bun run test`, CI workflow, README routes, seven
  investigation scripts pruned, demographics panel lazy-loaded (-362 KB).
- feat/camera-v2-retired: V2 model removed, saved `snv2` selections migrate to
  v3, V3 pure geometry into `camera-models/v3/geometry.ts`.
- feat/shader-precision: highp for moon, halo, shooting star; policy note.
- feat/ci-golden-drift: `CITY_GOLDEN=skip` in CI plus
  [[decision-cross-runtime-determinism]].
- feat/vendor-chunk-check: three.js duplication gone after V2 retirement.
- feat/atlas-cache: window atlas cached in IndexedDB, primed with the bundle.
- feat/vignette-lab: `/vignette-lab` scaffold, [[vignette-lab]].
- feat/traffic-journeys: minor-tier cars walk 3 to 6 connected roads,
  [[decision-traffic-journeys]]. Needs Taylor's live look in rural areas.
- feat/camera-copy: camera select copy after V2.

Found: the city golden differs between V8 and JavaScriptCore (one ULP of
`Math.cos` flips the lattice tilt tie-break). A seed shared from Chrome can
render a different city in Safari. Spike in flight on deterministic
transcendentals; fixing it re-rolls every seed once, Taylor's call.

Audits banked in the review page: settings IA (about 180 controls in 15
flat sections; recommend a Look/Studio split), UI system (six glass recipes,
HUD chips off-token, dialog off-token, sub-12 px prose), and the lead's read
of 16 captured screens.

Leftover registered worktrees for Taylor to remove by hand (merged, safe):
`.claude/worktrees/agent-{ac2ebe9721ad6697a,a655ddcd3c86b7ef6,a1c098dfd74a4a597,a5e77818356da04f7}`
and the scratchpad `bundle-check` worktree.

Spike result (`spike/deterministic-math`, unmerged): deterministic
sin/cos/atan2/exp/hypot in `lib/seed/dmath.ts` are bit-identical across Bun
and Node and fix the documented lattice tie-break, but full byte-identity
needs about 99 more call sites across 14 more files, and the roads phase
costs 73 to 81 percent more (total generation +25 percent). Options recorded
in [[decision-cross-runtime-determinism]]: fund the full sweep, or narrow
the golden contract to structural properties (building and district counts,
orientation), or accept per-engine cities. Taylor's call.

## Presentation batch (worktree agent)

Applied the review page's "UI system" audit item (six glass recipes, HUD
chips off-token, dialog off-token, sub-12 px prose) as a token-level pass:
two shared surface recipes (`GLASS`/`CARD`, `components/ui/FloatingPanel.tsx`),
HUD chips + `dialog.tsx` onto theme tokens, a 12px type floor, one tooltip
delay, focus rings on the raw column/directory buttons, reduced-motion
guards, and the seed chip re-cut as a legible share handle with a build-time
version stamp. No IA changes. Gates pass; screenshots under
`samples/presentation-batch/`.

- created [[decision-ui-surfaces-and-tokens]]
