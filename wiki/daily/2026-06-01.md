# 2026-06-01

## Mobile settings polish + per-district streetlights + top-down fit (shipped)

Batch shipped to `main` + GH Pages (deploy `26742895489` green, live 200):

- **Mobile settings panel** — fixed bottom cutoff (`h-screen` → `h-dvh` + `max-w-full`);
  removed the per-mode detail box; wide/bold header Pause/Resume (orbit → revolution,
  top-down → scene clock); fly-speed slider relocated into the Camera section.
- **Streetlights** — size + brightness controls (live uniforms, defaults 0.8/0.85);
  per-district colour temperature (#42): downtown cool-white → industrial sodium-amber,
  per-lamp jitter; highway/arterial uniform cool white.
- **Seed overlay** — tap-to-expand on mobile + 4 s collapse grace (hover gated to
  `pointerType "mouse"` so a tap's synthetic mouse events don't wedge it).
- **District overlay** — white border outline along every seam between districts.
- **Ground / haze** — ground radius 3000 (`1500 × CITY_SCALE`); haze re-centred on the
  world origin to align with the ground disc (same centre + radius).
- **Top-down** — frames the whole city in any aspect (orthoSize from `CITY_HALF_EXTENT`
  + viewport aspect) instead of a fixed clip.
- **Defaults** — city shape = circle; orbit elev 6 / azim 0 / focal-y 240; star intro
  240 s; streetlight intro 60 s; fog `#0b0d14` near 4800 far 7200; haze `#1b2641`
  top 240 bottom -30 strength 1 radius 3000.

Commits `6180de7` + `864fdaa` on `arch/city-planning` → merged `arch → dev → main`.

## Settings IA evaluation

Wrote [[settings-ia-evaluation]] — evaluation of the current 15-section flat panel +
five proposed layouts (audience tiers / domain groups / task framing / command-bar +
search / drag-drop `/studio` dashboard), comparison matrix, and a recommendation
(manifest refactor → ship D → build E with A–D as presets).

**Direction chosen: manifest → Option D.** Built v1 (uncommitted, `arch/city-planning`):
control manifest (`settingsManifest.ts`, 55 controls × group/audience/spec/get/set),
generic `ControlRow`, `SettingsPanel` (quick-bar + search + group chips + Basic/Studio/Dev
depth + footer), `SettingsRoot` (D ↔ classic swap, non-lossy). `SceneState` exported.
Verified live (search/chip/depth all work); tsc + lint clean. Composites (pose, window
profiles, render matrix, district list, readouts) still in the classic panel — port next,
then retire classic and build E on the same manifest. See the note's Build log.

## CI note

GH Pages deploy actions throw Node 20 deprecation warnings — GitHub forces Node 24 on
2026-06-16. `deploy-pages.yml` actions (`checkout@v4`, `configure-pages@v5`,
`upload-artifact@v4`, `deploy-pages@v4`) need a version bump before then.
