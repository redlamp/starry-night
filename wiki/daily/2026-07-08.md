# 2026-07-08

Overnight autonomous run (user asleep; goal set: persona system to indie-game production quality, Heat Signature as the hook-quality bar).

- Caught `fable` up to `dev` (clean fast-forward via `git fetch . dev:fable` — the git-gate blocks `branch -f` and this is the no-force idiom). New worktree `C:\workspace\starry-night-personas` on `feat/personas`, node_modules junctioned.
- Two research passes (games + academic/theory) → [[procgen-character-design-games]], [[procgen-character-theory-literature]]. Adopted the `#domain/procgen` tag for this content stream.
- Built the persona system v1: datasets → naming (city/districts/streets/addresses) → directory (households, employment, dating weaves) → story layer (authored template pools, lore, hooks, sift) → UI (occupants section + PersonaPanel). Full detail: [[decision-persona-architecture]], project tracker: [[personas]].
- Perf: address pass 19.4 s → 224 ms (spatial hash); directory ~500 ms cold / 0 warm; 23.5k personas at the 6 km tier.
- Quality-review fixes after first fanfic-test read: employment affinity ladder (no more bellhops at law firms), kid tenure inherits household move history, whyAwake/hook dedupe per household, epithet dedupe per building, address collision bump.
- Verified: personaCheck PASS (determinism, reciprocal links, no slot leaks), typecheck/lint clean, **city golden byte-identical**, CDP capture of both panels (`samples/personas/panels.png` — Owen T. Taylor, "the night guard", hook: "Walks past the 4 a.m. ferry on the way home. It is not on the way home.").
- Delegation per user guidance: research + panel UI on Sonnet agents (diffs reviewed); core generator + template authoring + integration on Fable.
- Everything left uncommitted on the worktree for morning review; exec-report artifact prepared.

## Round 2 (early morning, user briefly awake)

- Backlog burn-down on `feat/personas`: [[nameCohorts|SSA cohort-dated name tables]] (decade-weighted real SSA data, one-rng-draw guarantee), **domain words** (Grinblat: one obsession-noun per persona threaded through detail/hook/epithet, deduped per building, legend keeps crows), **cross-building family weave** (3,472 parent links matched by surname + parent-shaped age gap), **City Directory panel** (settings-drawer section: stats, seeded resident-of-the-night spotlight, name search, district → building browse; Sonnet-built, reviewed), whyAwake/kid/teen pool growth after the repetition audit (`scripts/personaAudit.ts`, hook max-repeat 857 → 591).
- **Commute layer** (user request): deterministic mode per employed persona (walk/cycle/transit/drive from distance + age + district), mode-coloured x-ray arc home→work when a persona is selected ([[decision-persona-architecture]] add-on: `components/scene/CommuteArc.tsx`), Commute row in PersonaPanel ("Drives · 1.8 km", transit names the actual line).
- **Road hover** (user request): inspect-mode ground-ray → segment-grid lookup (`roadQueryFor`), street-name chip + x-ray road highlight + buildings-on-street list (`components/scene/RoadHover.tsx`); logic verified headless (`scripts/roadQueryCheck.ts`, 7/7), hover feel awaits live test.
- Learned: `?capture=1` hides the HUD **by design** (captureMode gates CameraPanel/SeedControls/ControlsGuide) — chased a phantom crash for a while; UI captures must drop the capture param. `scripts/cdpConsole.ts` added for browser-console forensics.
- **Issues sweep** (user goal): closed #87 (info panel), #83 (shortest-arc top-down tween), #84 (blend size-breathing), #57 (traffic journeys) with evidence comments; #60 (/build bench) got a scoped implementation plan, left open; **#89 closed** after implementing v3 spline tours on `feat/heli-splines` — [[decision-heli-spline-tours]].
- Two more captures for the report: `samples/personas/directory.png`, `samples/personas/commute-arc.png`, `samples/heli-splines.png` (heli worktree).

## Round 3 (user awake and directing)

- **Entity columns** ([[decision-entity-columns]]): Miller-columns drill unifying district/street/building/company/persona panels — selection path + cursor in Zustand, three display states (side-by-side / CSS-3D deck / collapsed), true Miller branching in side view, back/forward without truncation. Old BuildingInfoPanel + PersonaPanel deleted (fully ported).
- **Schools**: 71 real schools at the default 6 km seed (46 elem / 16 middle / 9 high), 100% of kids 5-17 enrolled at the nearest tier school, walk/school-bus commutes (bus = new yellow arc mode), teachers hire in via kind matching.
- **Interconnection arcs**: thick mode-coloured commute arc + thin violet connection arcs (partner/family/relation homes) for the selected persona; street click opens the street column with its polyline highlighted x-ray.
- **Badge tooltips**: astrology + Chinese zodiac + MBTI badges explain themselves on hover (authored trait one-liners in personaData).
- Verified: typecheck/lint clean, personaCheck 15/15 + schools spot-check, captures `columns-side.png` / `columns-drill.png`.
- **Columns polish** (user feedback): docked snug top-left (headers aligned); row width capped by the live settings-drawer width (CameraPanel publishes it to the store) + gear reserve; horizontal scrollbar sits ABOVE the columns with wheel→horizontal panning on hover; fixed the base-ui ScrollArea wrapper to position scrollbars INSIDE panels (they flowed after the viewport — every card was silently unclippable); per-card shadcn scrollbars with taller max-height. Trait badges upgraded from title tooltips to real shadcn hover cards (`components/ui/hover-card.tsx`, base-ui PreviewCard).
- **Columns polish 2** (live-test feedback): horizontal scrollbar truly above the row (primitive composed directly — the shared ScrollBar's variant classes won the cascade and dropped it back to the bottom); Deck view reworked to per-card perspective pivoted on each card's right edge (the shared container perspective projected left cards upward out of the dock) with tilt+stack only, Y top-aligned; choppiness fixed by transitioning transform/filter only (margin animation = per-frame reflow); "+N more" lists now expand in place (ShowMore); the stack-collapses-to-one-card bug was the street-click handler stealing the path — now gated to canvas-origin primary clicks and it PUSHES onto an open stack instead of replacing it. All native title tooltips replaced: shadcn Tooltip for action labels (IconTip), hover cards for trait badges. Focus/Go Home/Go to Work fly-tos now settle at a 45° look-down (polar-only tween keeps the #87 no-revolve rule).
- **Deck tabs + focus framing**: deck slivers became uniform-height tabs with a fade-out mask (natural heights sheared mid-text when tilted — user screenshot); deck row got vertical breathing room for the projection. Focus/Go Home/Go to Work fly-tos settle at the 45° look-down with the building spanning ~33% of display height (FOCUS_HEIGHT_FRACTION, both projections).

## Round 4 (morning, live iteration with user)

- **Cone / Follow Locations**: header toggle (lucide Cone) that frames every location tied to the top card — persona: home+work+connections' homes; company/school: building+staff/student homes (catchment); building: occupants' destinations + workers' origins; street: full run; district: bounds. Follow mode re-frames on every drill move. Framing: centroid + 85th-percentile radius (outliers stay visible as arcs, can't blow out the zoom), fill-frame fit (`focusRequest.fit: "fill"`) vs the 33%-height rule that stays for double-click focus.
- **Employment arcs**: topmost building/company cards draw thin violet arcs workplace→each employee's home (CommuteArc follows the top entity column now).
- **Deck iteration with user**: tilt direction restored (fanned toward viewer, moderate 11–26°/1200px — the negated fold read "wrong direction"); uniform tab slivers + max-height tween already in.
- **Stat rows**: ColumnStat `stack` mode — long Profession/Commute/Education/Home/Status values break label-above/value-below (>26 chars).
- **Header split**: kind chip + nav on row 1, full-width wrapping title on row 2 (long company/building names no longer crop).
- **Focus framing**: pin/home/work fly-tos settle at 45° look-down, building at ~33% display height.
- **Writing lab** (in flight): content pools exported from personaStory/naming/personas; `lib/writing/contentRegistry.ts` (~50 pools) + `labStore.ts` (localStorage overrides: author ai/human/edited, status draft/review/final/cut; export-as-TS ship path); /writing-lab page delegated to Sonnet.
- **/writing-lab shipped**: content-review workbench over all ~50 pools / ~947 entries (story templates, names, professions, places, business templates, trait readings). Columns: content (click-to-edit), author (ai/human/edited, auto-flips on edit), status (draft/review/final/cut — cut strikes and drops from export). localStorage persistence + "Copy Pool as TS" ship path (paste over the source const). Sonnet-built against my registry/store contract; the agent caught a real id-collision bug in my registry (story.hooks.generic vs the cluster loop) — fixed + verified. All content defaults ai/draft.
- **/writing-lab design pass** (Sonnet, TMS research first): Lokalise/Crowdin-style sidebar (collapsible groups with pool/entry counts + finalized-progress bars, Hooks nested as its own 19-pool subsection, pool filter), Airtable-style resizable Author/Status columns (pointer-capture handles, widths persisted in a separate ui.v1 localStorage key), colored-dot Select dropdowns for author/status, dotted Badge status chips, global cross-pool entry search with jump-and-flash. Agent's sub-fork went off-brief and created/deleted scaffold files — independent audit confirmed zero damage (change list matches expected set exactly; only trace is a gitignored tsbuildinfo). All gates green: typecheck, lint, personaCheck 15/15, golden. Drag-resize feel = live user gate.
- **Apophenia engine** captured as canonical wiki note ([[apophenia-engine]]) + artifact section; report stored in-repo at docs/reports/personas-report-2026-07-08.html (2.2 MB — slimming decision pending pre-merge).
- **Writing lab round 3** (user-directed): resizable pools column (drag divider, persisted), group color coding (Story violet / Names amber / Places green / Businesses blue / Traits rose — dots on groups, accent border on selection), sidebar toolbar (Expand All / Collapse All / Locate Selected), pools collapsed by default, How It Works tutorial dialog (proper dialog-kit composition — bare DialogContent renders inline), shared ThemeToggle extracted from CameraPanel (light/gray/dark now switchable from the lab). Report slimmed for git: repo copy text-only (~60 KB), full 2.2 MB illustrated version kept local in gitignored samples/.
