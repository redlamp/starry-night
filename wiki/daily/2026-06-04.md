# 2026-06-04

## #58 — city size tiers + crop split (shipped to working tree, verified)

- Backlog sweep first: filed **#55** (per-tile culling + lazy materialisation),
  **#56** (camera-follows-crop), **#57** (traffic streams) out of the wiki's #14
  Steps 5–7; then **#58** (size tiers) + **#59** (progressive gen) + **#60**
  (`/build` stepper) from the initial-processing-demand discussion. #14 commented
  as the umbrella.
- **#58 implemented**: `MAX_HALF_EXTENT`/`GEN_SCALE` → runtime `maxHalfExtent()` /
  `genScale()` + `setCityTier()` (town 750 / city 1500 / metro 3000 half-extent);
  gen constants derive per-call; caches key on extent; store `citySize` +
  `cropLock` persisted; notched size slider (drag = preview only, **re-roll on
  release**), lock-crop-to-size checkbox; 9 gen consumers re-key on tier; scripts
  pinned to Metro.
- **Verified**: GOLDEN PASS (byte-identical at Metro — refactor changed no gen
  math), CROSSCROP PASS, GATE 1 PASS, tier sanity PASS. **Default boot gen ~8 s →
  ~0.85 s** (City tier). One-time default-view shift (City-tier gen ≠ Metro
  cropped) — accepted.
- User visual check: **OK**.

## Parked feedback — districts (user, for a later pass)

Comments pending on: **building sizes within districts**, **building
distribution**, and **how district characters are applied across the city**.
Known related tuning note: at Town tier the downtown band (650 m) covers most of
the 750 m disc. Pick this up alongside the #56/#54 look pass or as its own
district-tuning session — builds on [[decision-density-gradient-bands]].
