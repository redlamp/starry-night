# 2026-06-04

## #58 — city size tiers + crop split (shipped to working tree, verified)

- Backlog sweep first: filed **#55** (per-tile culling + lazy materialisation),
  **#56** (camera-follows-crop), **#57** (traffic streams) out of the wiki's #14
  Steps 5–7; then **#58** (size tiers) + **#59** (progressive gen) + **#60**
  (`/build` stepper) from the initial-processing-demand discussion. #14 commented
  as the umbrella.
- **#58 implemented**: `MAX_HALF_EXTENT`/`GEN_SCALE` → runtime `maxHalfExtent()` /
  `genScale()` + `setCityTier()` (town 750 / city 1500 / metro 3000 half-extent);
  gen constants derive per-call; caches key on extent; store `citySize` +
  `cropLock` persisted; notched size slider (drag = preview only, **re-roll on
  release**), lock-crop-to-size checkbox; 9 gen consumers re-key on tier; scripts
  pinned to Metro.
- **Verified**: GOLDEN PASS (byte-identical at Metro — refactor changed no gen
  math), CROSSCROP PASS, GATE 1 PASS, tier sanity PASS. **Default boot gen ~8 s →
  ~0.85 s** (City tier). One-time default-view shift (City-tier gen ≠ Metro
  cropped) — accepted.
- User visual check: **OK**.

## Evening — sketch → tensor → city (#40), tile culling (#55), shipped to /dev

- **Sketch→field prototype**: user's hand-hatched notebook photo → structure
  tensor recovery (orientation + coherence per cell) + sign-continuity BFS for
  implied flow; then RK4 street trace through the recovered field
  (`scripts/sketchField.ts` / `sketchTrace.ts`). Whirl page → ring roads +
  spokes. Created [[decision-sketch-tensor-field]].
- **`/tensor` lab page**: drop/paste a sketch, recovery + trace knobs
  (slider + base-ui NumberField steppers, scrub-able labels), 5 layer toggles,
  drag-resizable sidebar, **Use in city** bridge. New shadcn-style
  `number-field` wrapper.
- **#40 shipped**: registered sketch drives the WHOLE city (field swap at
  `buildTensorRoadsImpl` + ink = footprint); worker carries the sketch;
  caches key on content hash. Golden ×10 byte-identical without a sketch.
- **#55 shipped (culling half)**: per-tile buffer compaction for buildings /
  streetlights / traffic — created [[decision-tile-cull-materialisation]].
  `tileCullSanity` PASS; 3080 Ti fps fine (user); **Pixel 6 pass pending**.
  Wake-reveal half stays open on #55.
- **FPS badge**: persistent top-left fps + draw-calls HUD, Performance-section
  toggle (`8b0326a`).
- **Housekeeping**: #59 closed (A+B shipped; reveal ideas → #55); spike→dev→main
  promotion done yesterday; today dev pushed (8 commits) + Pages workflow
  dispatched — `/` (main, unchanged) + `/dev` (all of the above) live.
- R3F 10 still alpha (#43 parked).

## Late evening — freeways (#13), issue sweep, #51/#50, sky pass (#26)

- **#13 shipped + closed**: freeway network with per-seed character (tier =
  ceiling town 1 / city 2 / metro 3, seed rolls the count — towns can have 0),
  same-corridor dedupe only (angles from the field: criss-cross, diagonal,
  offset-parallel pairs), `maxWindowTurn` radial-pull guard (≤35° per 200m ≈
  330m min radius — fixed the plan-3 plaza hook), interchange glow (cloverleaf
  ramp-light loops at highway crossings). Golden recaptured ×2 along the way.
- **Issue sweep**: closed #14 (umbrella), #40 (shipped as /tensor +
  sketch-cities), #59 (earlier), #13, then #51 + #50; #60 title fixed (MSYS
  mangling); #2 parked for a visual check; #43 parked (R3F 10 still alpha).
- **#51 deviation slider**: runtime ×0.25–2 scale on each seed's rolled field
  deviation + shear angle (Debug View, regen-on-release, persisted,
  worker-aware; ×1 byte-identical). Closed; "go further" = future issue.
- **#50 interior fill**: road-aligned buildings in the 18–55m interior band,
  per-character density, own rng stream (frontage identical); +7–11%
  buildings; `scripts/blockZoom.ts`. Closed; true lot polygons = future issue.
- **#26 research + sky slices 1–2**: agent research →
  [[night-sky-reference-real]] + [[night-sky-reference-stylized]] (original
  decoded from `starryn` C source). Shipped: 3-stop dithered indigo gradient +
  warm skyglow band; magnitude-law star brightness (3×/band, 2.512× flux in
  light); quadratic desaturation; elevation-driven twinkle; 1–3 steady warm
  planets; hero-star Van Gogh halos; rare shader-clocked shooting stars
  (~every 40s). Open remainder: moon-phase model → full-moon star-cut +
  true 21.84° halo ring.
- **FPS badge** (`8b0326a`): persistent top-left HUD, Performance toggle.
- Deploys: `/dev` redeployed twice (through `60dec55`); slice 2 (`582a6ad`)
  not yet pushed. **Pixel 6 pass on #55 still pending.**

## Parked feedback — districts (user, for a later pass)

Comments pending on: **building sizes within districts**, **building
distribution**, and **how district characters are applied across the city**.
Known related tuning note: at Town tier the downtown band (650 m) covers most of
the 750 m disc. Pick this up alongside the #56/#54 look pass or as its own
district-tuning session — builds on [[decision-density-gradient-bands]].
